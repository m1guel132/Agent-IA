---
status: living
title: Product Backlog
source: governance-bloat-review-2026-04-12 + optimization-round-2026-05-04 + optimization-research-2026-06-02
created: 2026-04-12
last_updated: 2026-08-24
---

# Product Backlog

> **Completed entries** (Shipped / Cancelled) live in
> [`_product-backlog-archive.md`](./_product-backlog-archive.md). This index
> tracks only active (Pending / In Progress) work. Entry numbers are global and
> stable across both files, so `#N` dependency references resolve in either.

## Source Summary

Governance file bloat review (2026-04-12) identified 10 findings across P0–P2: multiple data surfaces grow unbounded (archive/, Global Lessons, Spec Index, INDEX.jsonl), compaction mechanisms are advisory-only, and process artifacts (_raw-intake archives, superseded L2 entries, shipped specs) accumulate without consumers. Industry patterns (LSM-tree compaction, progressive summarization, tiered retention) converge on a 4-state document lifecycle with LLM-driven summarization at tier transitions.

## Feature Inventory

> The **GH Issue** column links the GitHub tracker (`KbWen/agentic-os`). The
> **Dependencies** column uses bare `#N` = backlog entry numbers (this file).
> Entries #45–#68 came from a 2026-06-02 optimization-research pass mining a more
> mature internal reference implementation. The set was deliberately curated: each
> survivor was cross-checked against this repo's actual code paths over multiple
> verification rounds; items that were premature, already-handled, or
> deliberately-removed-before were dropped (DELETE-bias). The rows below are the
> verified ~1–2 month roadmap. See
> [`../OPTIMIZATION_ROADMAP.md`](../OPTIMIZATION_ROADMAP.md).

| # | Feature | Kind | Labels | Priority | Spec File | Tier | Status | GH Issue | Dependencies |
|---|---|---|---|---|---|---|---|---|---|
| 1 | Tiered Document Lifecycle Engine (4-tier state machine + config) | framework | lifecycle | P1 | docs/specs/tiered-doc-lifecycle.md | feature | Pending | [#140](https://github.com/KbWen/agentic-os/issues/140) | — |
| 3 | Archive directory GC + INDEX.jsonl rotation 2026-07-10 evidence: INDEX.jsonl at 99 lines / archive/ 106 files 1.3MB at month ~3; compaction prohibited by the append-only witness — rotation design must be witness-compatible. | framework | lifecycle | P2 | — | feature | Pending | [#141](https://github.com/KbWen/agentic-os/issues/141) | #1 |
| 7 | Domain Doc L2 superseded entry archival | framework | lifecycle | P2 | — | quick-win | Pending | [#142](https://github.com/KbWen/agentic-os/issues/142) | — |
| 11 | Shipped specs accumulation — status-driven filtering | framework | lifecycle | P2 | — | quick-win | Pending | [#143](https://github.com/KbWen/agentic-os/issues/143) | #1 |
| 13 | Warm→Cold LLM summarization pass in /ship | framework | lifecycle | P2 | — | feature | Pending | [#144](https://github.com/KbWen/agentic-os/issues/144) | #1, #3 |
| 18 | Lightweight routing heuristics (decision tree in config.yaml, not a DSL) | framework | routing | P2 | — | quick-win | Pending | [#148](https://github.com/KbWen/agentic-os/issues/148) | — |
| 21 | Skill cache timestamp + staleness invalidation | framework | skills | P2 | — | quick-win | Pending | [#149](https://github.com/KbWen/agentic-os/issues/149) | — |
| 33 | Claude Code plugin packaging (.claude-plugin/plugin.json + bin/ + commands/agents/hooks bundling, no monitors) | dx | packaging | P2 | — | feature | Pending | [#150](https://github.com/KbWen/agentic-os/issues/150) | #30, #31 |
| 70 | Export structured JSON Drift Log on ship for CI/CD audit trails | framework | governance | P2 | — | feature | Pending | [#193](https://github.com/KbWen/agentic-os/issues/193) | — |
| 69 | RPI→QRSPI flow-adaptation study — Strand D executed as directive-enforcement-audit (spec + ADR-011 + 112-row census + count ratchet); strands A/B/C/E remain open as future candidates | framework | governance | P1 | docs/specs/directive-enforcement-audit.md | feature | Shipped | [#176](https://github.com/KbWen/agentic-os/issues/176) | #45, #65 |
| 72 | zh-TW README FAQ mirror (parity with EN discoverability FAQ added in PR #230) | docs | i18n | P3 | — | quick-win | Shipped | — | — |
| 76 | Research persist-before-browse note (lightweight `research.md` convention; capsule design retired per external prior-art) | framework | governance | P2 | — | quick-win | Shipped | [#251](https://github.com/KbWen/agentic-os/issues/251) | — |
| 77 | D — Reviewable Task→Step hierarchy | framework | governance | P1 | — | architecture-change | Pending | [#252](https://github.com/KbWen/agentic-os/issues/252) | — |
| 78 | B+C — Task capsule + one read-only reviewer (two verdicts) | framework | governance | P1 | — | feature | Pending | [#253](https://github.com/KbWen/agentic-os/issues/253) | #77 |
| 79 | A — Skill effectiveness evaluation harness. **2026-08-11 split**: the trigger-accuracy half moved to #165 ([#398](https://github.com/KbWen/agentic-os/issues/398)) because it is deterministic against `trigger-registry.yaml` and needs neither #77 nor #78; THIS row is now the effectiveness half only (A/B skill-on vs skill-off pairing, repeated runs + aggregation, isolation mode, measured token/time/tool metrics, separate human-feedback channel), which is genuinely gated on #77/#78. Open design question recorded at triage: `run_governance_eval.py` `--agent-cmd` captures stdout only, so measured token/time must come from a harness that reports them or be recorded as explicit `unknown` — do NOT synthesize estimates, and do NOT conflate `analyze_token_lifecycle.py` (static estimates) with measured runtime cost. | framework | skills | P2 | — | feature | Pending | [#254](https://github.com/KbWen/agentic-os/issues/254) | #77, #78 |
| 80 | G1a — First-party SKILL.md compatibility floor (delta over validate.sh) | framework | skills | P1 | — | quick-win | Shipped | [#255](https://github.com/KbWen/agentic-os/issues/255) | — |
| 81 | G1b — Activated-skill provenance inventory | framework | skills | P2 | — | quick-win | Shipped | [#256](https://github.com/KbWen/agentic-os/issues/256) | — |
| 82 | Karpathy-principles source/license provenance wording | docs | governance | P3 | — | tiny-fix | Shipped | [#257](https://github.com/KbWen/agentic-os/issues/257) | — |
| 83 | Skill/workflow content-optimization pass — research + compare vs reference repos (multica-ai/addyosmani/tech-leads-club/VoltAgent/superpowers/anthropic-skills) for substantive guidance/structure improvements to our skills & workflows | research | skills | P2 | — | research | Cancelled | — | after #80/#81/#82 ship |
| 84 | README→CI onboarding — show adopters how to wire CI as a required, non-bypassable check; README sells "CI is the floor that can't be skipped" but Quick Start only covers `deploy_brain.sh` (reviewer-flagged as the real conversion gap; likely a docs/INSTALL.md section + branch-protection recipe) | docs | adoption | P2 | — | quick-win | Shipped | — | after #262 |
| 85 | Cursor first-class support — a real in-Cursor screenshot/GIF + a Cursor platform guide (Cursor sits in the Compatible tier with no guide while Codex/Claude have one; `CLAUDE_PLATFORM_GUIDE.md` also lacks a `_zh-TW` twin) | docs | adoption | P3 | — | quick-win | Cancelled | — | after #262 |
| 86 | README polish — de-dup the 3× "leaked secret / fake tests / skipped phase" repetition (Rules-vs-enforcement · What-you-get · FAQ) and the 2× cross-platform mention; unify the workflow + pipeline GIFs onto one example task (EN + 繁中) | docs | adoption | P3 | — | tiny-fix | Shipped | — | after #262 |
| 87 | INSTALL zh-TW twin — `docs/INSTALL.md` (incl. the #84 CI-floor onboarding section) is EN-only; create a 繁中 twin for locale parity (Codex/Claude platform guides + EN/繁中 READMEs all have zh twins; INSTALL doesn't) | docs | i18n | P3 | — | quick-win | Pending | — | after #84 |
| 88 | Balance Windows pytest shards via a committed `.test_durations` file (count-split clusters the deploy tests onto one shard). **Measurement corrected 2026-08-23**: this row recorded `7:14`, but the real PR #417 job times were **21m57s / 3m19s / 4m14s** — the imbalance had grown ~3× since the row was written, with the worst shard carrying 74% of all the work | dx | ci | P3 | — | quick-win | Shipped | — | Shipped 2026-08-23 (branch perf/test-durations-shard-balance-88): 897-entry `.test_durations` committed; **measured on PR #419, two runs**: worst shard **21m57s -> 13m7s then 11m24s** = **1.67x-1.93x** (shards 7m18s/8m28s/13m7s pre-rebase, 6m56s/6m43s/11m24s post-rebase); worst share 74% -> ~45% (ideal 33.3%). Runner variance between the two runs is real, so the win is quoted as a range rather than as the better number. The predicted 2.2x was NOT achieved: durations were generated on a workstation, not on `windows-latest`, and the runner weights small tests relatively higher (the 640-test shard became the slowest). **Follow-up**: regenerate durations on the runner via a `workflow_dispatch` job with `--store-durations` + artifact upload to approach the 9.6-min ideal. `least_duration` measured better (19.1/19.1/19.1) but rejected — cross-group reordering risk not worth 2.6%. |
| 89 | Work Log enforcement audit — validator checks `## Security Findings` section presence on review/ship logs (C1) + Loaded-Sections receipt presence in `## Session Info` (C2); both honor-system today (every MUST = one enforcement) | framework | governance | P2 | — | quick-win | Shipped | [#288](https://github.com/KbWen/agentic-os/issues/288) | Shipped via PR #345 (2026-07-17); backlog reconciled 2026-07-22 |
| 90 | BOM-tolerance for the central `_yaml_loader.py` (read `utf-8-sig`; parity with the #273 capabilities/provenance BOM fix — the authoritative loader still reads trigger-registry/lifecycle-scenarios as plain utf-8) | framework | governance | P3 | — | quick-win | Pending | — | — |
| 91 | Fix `retro.md` Global-Lessons append misplacement — guard exposes only file-end `--mode append`, but `## Global Lessons` is mid-file so the lesson lands under Ship History (same class as the shipped #265 ship.md fix); switch to section-insert | framework | governance | P3 | — | quick-win | Shipped | — | PR #322 (root-caused as retro.md §3 doc defect; tool already section-locates) |
| 92 | `--list-checks` parity — add `-ListChecks` to `validate.ps1` OR correct `CHANGELOG.md:217` which advertises it jointly for both validators (ps1 lacks it); decide enforce-vs-honest-doc at implement | framework | tooling | P3 | — | quick-win | Pending | — | — |
| 93 | `connecting-a-knowledge-base_zh-TW.md` twin — the zh-TW README `## 文件` table links this EN-only guide (the only row pointing at English); locale parity (sibling to #87) | docs | i18n | P3 | — | quick-win | Pending | — | — |
| 94 | Rename drifted lifecycle-token test names — `test_quick_win_current_total_under_29k` asserts 30000 and `..._under_350k` asserts 354000 (name/assertion mismatch) **Reconciled 2026-08-09: already shipped 2026-07-02 via PR #308 (disk-verified — `test_lifecycle_token_consumption.py:427` `..._under_30k`, `:499` `..._under_355k`); this row was never flipped.** | framework | test-hygiene | P3 | — | tiny-fix | Shipped | — | PR #308 |
| 95 | Delete orphaned + duplicate `docs/guides/superpowers-playbook.md` (+ its `.agent/workflows/` stub) — content duplicates the phase tables already in reference.md/READMEs; reachable only via the stub | docs | cleanup | P3 | — | tiny-fix | Shipped | — | PR #321 |
| 96 | Link `docs/ai-contributors.md` from a human-facing index (README Docs table or CONTRIBUTING) — human-facing contributor guide currently reachable only from copilot-instructions + a guard test | docs | discoverability | P3 | — | tiny-fix | Pending | — | — |
| 97 | routing_actions staleness escalation — `validate.sh`/`.ps1` WARN on `status: pending` routing actions older than N days (config-driven, parallel to existing lock + spec-index staleness checks) so a cross-domain orphan can't sit unwatched; today routing_actions get field-validation + a ship-time check only for pending actions targeting the *current* primary_domain | framework | governance | P3 | — | quick-win | Shipped | [#306](https://github.com/KbWen/agentic-os/pull/306) | — |
| 98 | SSoT section-targeted append for Spec Index + Ship History — `/ship` writes `current_state.md` via whole-file CAS replace + manual §11.1 additive-merge; the only structural many-concurrent-ship bottleneck for large teams. Convert these append-only sections to guarded `--mode append` (already proven, 50 concurrent → 50 lines). Backlog-only: no verified consumer today (solo repo, zero concurrent ship) | framework | governance | P3 | — | quick-win | Pending | — | — |
| 99 | Partial / minimal adoption on-ramp — deploy is all-or-nothing (~2600 files); no `deploy --safety-only` to take just the credential floor + safety nucleus (already factored standalone per ADR-008). Doc-first ("copy these 3 files") may discharge it; selector only if a real adopter wants floor-without-workflow | dx | adoption | P3 | — | quick-win | Pending | — | — |
| 100 | Subagent fan-out under autopilot: same-owner Work Log lock contention — `subagent_policy: read-only` avoids it; lock short-circuit deferred (ADR-007, no consumer). **2026-08-17 refute-first review (3 lenses, via #291) — session-reuse REFUTED, real defect relocated:** the proposed "subagents reuse the primary's `--session`" convention IS the deferred short-circuit in a more permissive form — `ensure` matches owner+session (`recover_worklog_lock.py:307`) and so does `release_lock` (`:354`), so the first subagent reaching /handoff or /ship deletes the lock out from under live siblings at exit 0 (release failure is WARN-only). It is also unimplementable (`--session` is a wall-clock stamp per `.agentcortex/templates/worklog.md:32`; no propagation channel — `.claude/agents/acx-*.md` say "DO NOT add logic here") and unverifiable (see #122: lock acquisition is never verified anywhere). **Do NOT build any lock change or session convention.** **REAL DEFECT:** the framework contradicts ADR-007 — ADR-007 states that under `read-only` "subagents never acquire it", yet the five `acx-*` shims say "Execute `.agent/workflows/<phase>.md` verbatim", `shared-contracts.md:19` makes phase-entry `ensure` unconditional, and `review.md` flags non-shim dispatch as a MEDIUM defect; following the framework's own dispatch guidance therefore puts subagents inside governed phases holding the lock (`acx-shipper` sharpest: `ship.md` writes SSoT and releases the lock). Deliverable = ADR-007 amendment deciding whether `acx-*` shims are read-only evidence producers or governed writers, plus one explicit lock-ownership line per shim file (`.codex` half blocked by #147). Also uncovered: same-owner + different-session — exit 2 is UNTESTED (`tests/guard/test_worklog_lock_blocking.py` fixtures: holder `other/other-session`, caller `claude/claude-session`, so `:256` covers different-owner and `:108` covers same-session); add that regression test. **REOPEN TRIGGER:** a real autopilot adopter reports a Work Log lock FAIL during parallel subagent fan-out, supplying (a) harness plus version, (b) fan-out shape, (c) the exact `ensure` exit-2 payload, and (d) the `--owner`/`--session` values each subagent passed. | framework | governance | P3 | — | architecture-change | Pending | [#291](https://github.com/KbWen/agentic-os/issues/291) | — |
| 101 | Surface adopter's pre-existing skills + the `custom-*` on-ramp at bootstrap — present-only opt-in detector (never auto-activate, ADR-007). Report-trigger issue for adopters with existing skill packs | framework | skill | P3 | — | feature | Pending | [#292](https://github.com/KbWen/agentic-os/issues/292) | — |
| 102 | Legacy poisoned-manifest migration (`baseline_mode: upstream`) — conditional follow-on to the v1.8.4 deploy data-loss fix: manifests written by ≤v1.8.3 may carry a preserved *user* hash as the baseline; a `baseline_mode` marker + one conservative sidecar cycle for unmarked manifests would stop a poisoned baseline from overwriting user content on the next upgrade. Backlog-only (needs a real ≤v1.8.3 install hitting it); the v1.8.4 fix stops NEW poisoning | framework | deploy | P3 | — | quick-win | Pending | — | after v1.8.4 |
| 103 | Governance premortem 2026-07-01 follow-ups — the 4 `status: pending` routing_actions in `docs/reviews/2026-07-01-governance-premortem.md`: (a) collision-resistant Work Log keys / explicit-resume confirmation for default/protected branches; (b) explicit reduced-assurance receipt labeling for ship/gate claims on no-file-access platforms; (c) ship-time risk-reconciliation check for quick-win governance/deploy/trust-boundary changes; (d) validator WARN taxonomy split (hygiene vs governance-integrity debt vs reduced-assurance). Keep each routing_action `pending` until absorbed into `docs/architecture/document-governance.md` or rejected. Backlog-only (premortem findings, no observed incident) | framework | governance | P3 | — | quick-win | Pending | — | — |
| 104 | Governance self-audit workflow — there is no dedicated phase/workflow for auditing the governance system ITSELF; `/audit` is scoped to legacy-repo onboarding ("map an existing legacy repository", routing trigger "new module, no ADR"), so governance premortems are run ad-hoc by overloading `/audit` (see the two 2026-07-01 premortem snapshots + the 2026-07-02 two-wave self-audit). A `/govern-audit` (or `/premortem`) workflow would give it a first-class, repeatable home with routing_actions + snapshot naming baked in. From 2026-07-02 self-audit (A7) | framework | governance | P3 | docs/specs/governance-self-audit-workflow.md | feature | Shipped | — | PR #311 |
| 105 | Deploy version-drift signal — downstream deploy records `version` in `.agentcortex-manifest` but nothing ever compares it to the framework version, so a fork running a months-old validator gets no "you are N versions behind" signal. Add an advisory drift check (validator WARN or deploy notice) comparing manifest version to the shipped banner. From 2026-07-02 self-audit (deploy agent) 2026-07-10: also cover the non-transactional deploy half-upgrade state (manifest written last; interrupted deploy leaves mixed-version files with a stale manifest and nothing detects it). | framework | deploy | P3 | — | quick-win | Pending | — | — |
| 106 | Work-log local credential scan — `scan_credentials.py` runs on the git diff/staged set, but `.agentcortex/context/work/*.md` is gitignored, so a secret pasted into a Work Log is never scanned by CI or the (opt-in) pre-commit hook. Add an opt-in local/pre-commit scan pass over `work/` (local-only; CI can't see gitignored logs). Low blast radius (local ephemeral), honesty-motivated. From 2026-07-02 self-audit (seams agent) | framework | security | P3 | — | quick-win | Pending | — | — |
| 107 | Eval coverage partial-match precision — `run_governance_eval.py` `_run_coverage` matches a case's `protects:` to the rule inventory via a bidirectional substring test (`norm_protects in rule OR rule in norm_protects`), which can mis-attribute a case to a longer/shorter anchor and over/under-count coverage. Tighten to exact-or-explicit-prefix. Also normalize the one `### §4.5` heading (literal § → inventory emits `§§4.5`) so the anchor stops being a special case (update the guarding case's `protects:` in the same change). From 2026-07-02 self-audit (eval agent) | framework | tooling | P3 | — | quick-win | Shipped | — | Shipped 2026-07-22 (branch fix/eval-coverage-precision) |
| 108 | `verify_agent_evidence.py` `parse_evidence()` accepts incomplete stanzas — an entry with a `command:` but no `result:`/`summary:` is saved as `EvidenceEntry(..., result='', summary='')` instead of being rejected/flagged. Add a completeness guard so partial evidence stanzas are surfaced, not silently accepted. From 2026-07-02 self-audit (python-tools agent) | framework | tooling | P3 | — | tiny-fix | Pending | — | — |
| 109 | Reclassification has no timing guard — completeness/gate checks use the FINAL classification, and the H4 reset is count-based on Drift Log records with no check that the reclassification happened BEFORE implement; a post-implement feature→quick-win downgrade can shed the review/handoff required-set. Related to #103(c) quick-win risk reconciliation. Local-only enforcement (work/ gitignored, CI-invisible). From 2026-07-02 self-audit (gate-bypass agent, A3) | framework | governance | P3 | — | quick-win | Pending | — | — |
| 110 | Hotfix no-test-runner dead-end — `test.md` correctly refuses a PASS test receipt when no test runner exists (even with user sign-off), but `ship.md` requires a test receipt for hotfix → an emergency fix on a runnerless host has NO legal path to ship. Needs a policy decision: reduced-assurance hotfix ship lane (explicit label + user sign-off) vs escalate-to-feature mandate. From 2026-07-02 wave-3 walkthrough (C3) | framework | governance | P2 | — | quick-win | Pending | — | — |
| 111 | Test-teeth hardening batch — from the 2026-07-02 test-quality audit: (a) AC-8 credential-scan tests: `test_ac8_rc3_not_exit_zero` block-end detection coupled to exact YAML indentation, `test_ac8_base_sha_missing_still_exits_zero` over-broad substring; (b) `test_codex_and_claude_deltas_differ` asserts nothing its name claims; (c) framework-live-state fixtures (#170/#172 no-warn tests) should pin to a deployed temp fixture; (d) verify windows-latest runner actually has Git-Bash (else all sh↔ps1 behavioral parity tests silently skip everywhere). Batch of assertion-precision fixes, no engine change 2026-07-10 additions: (e) validate.ps1 illegal-gate-progression has NO FAIL-path behavioral test (only the legal-case ps1 sibling exists — test_validator_false_positives.py:1019/:1045); (f) PR #314's F1/F2 fixes are guarded by source-substring asserts only, never executed behaviorally (:841-867) while adjacent D4 checks show the behavioral pattern (:886-910); (g) tests/guard/test_ssot_heartbeat_contract.py:7-8 docstring falsely claims .agentcortex/tests/test_ssot_completeness.py is not CI-run (collect-only proves it is). | framework | test-hygiene | P3 | — | quick-win | Pending | — | — |
| 112 | CI docs-only gate hole — README/INSTALL/spec content-pin tests (`test_readme_fork_guidance_parity`, `test_text_only_install_does_not_copy_runtime_context`, `test_ac4_ac5_readme_documents_pre_commit_hook_setup`, `TestClaimsVsReality`) live in pytest jobs gated `heavy == 'true'`, and README/docs paths classify inert → the pins never run on the docs-only PRs most likely to break them. Fix direction: a fast ungated "docs-pins" job (pure file-read tests, seconds) that runs when heavy=false. Also: audit-chain git append-only witness has NO behavioral test (only source-substring presence) — add slow behavioral tests (truncated/edited INDEX in a temp git repo). From 2026-07-02 test-quality audit (both P1s) | framework | ci | P2 | — | quick-win | Shipped | — | PR #310 |
| 113 | `--no-python` validator undersells reduced assurance — the bash gate-progression fallback checks only that plan+implement receipts are PRESENT, not phase ordering/completeness, so in `--no-python` mode a feature Work Log that skips review/test/handoff (but keeps plan+implement) prints top-line "integrity check passed". The only tell is the SKIP count jump (13 vs 4). Fix direction: the fallback can grep required-receipt PRESENCE per classification (no Python needed for presence-counting, only for ordering-legality), OR the top-line verdict should say "reduced assurance" when python is off. Connects #103b reduced-assurance labeling. From 2026-07-02 stability-sim red-team (Attack 9) 2026-07-10: also a cross-validator verdict divergence — on a no-Python host validate.sh degrades gate-progression to the narrow ship-missing-prereq fallback (WARN) while validate.ps1 runs the full native parser (FAIL) → same log, different verdicts by platform. | framework | governance | P2 | — | quick-win | Shipped | — | branch fix/no-python-reduced-assurance (reduced-assurance top-line label + malformed-log corpus) |
| 114 | tiny-fix self-labeling has no validator visibility — a large semantic change mislabeled `tiny-fix` ships with one receipt and a clean pass because tiny-fix has NO Work Log (validator never sees it) and `required=set()`. No file-count/diff-size check exists. Honor-system limit inherent to the no-worklog fast path; a real guard would need a pre-commit/CI diff-size heuristic outside the work-log validator. Related to #103c quick-win pressure-valve. From 2026-07-02 stability-sim red-team (Attack 3) | framework | governance | P3 | — | quick-win | Pending | — | — |
| 116 | check_command_sync EXPECTED_COMMANDS coverage gap — the checker tracks 27 of 30 `.claude/commands/` stubs (`app-init`, `execute-plan`, `write-plan` untracked), so a deployed adopter counting files vs the printed "N commands verified" sees a confusing mismatch; decide track-all vs document-exclusions at implement. Pre-existing, surfaced by the PR #316 fresh-adopter deploy sim | framework | tooling | P3 | — | quick-win | Shipped | — | PR #321 |
| 117 | Global Lessons cap-archival vs hash-chain contradiction — retro.md §3 says archive oldest LOW entries at cap, but check_lesson_chain.py breaks on ANY removal (head must be GENESIS-anchored; each prev hashes its immediate predecessor), so the documented procedure is unexecutable; append_lesson.py refuses at cap (hit for the first time 2026-07-04 at 20/20). Decide: chain-aware archival (move entry + re-anchor successor + audit record in archive) vs cap bump vs HIGH-pin-only. Blocks new Global Lessons until resolved | framework | governance | P2 | — | quick-win | Shipped | — | PR #322 (chain-aware archival; residual: append the pending 2026-07-04 lesson via /retro) |
| 118 | Claude adapter bootstrap hard-stop sync — `.claude/commands/bootstrap.md` unconditionally "STOP and ask" even when the user explicitly requested a downstream phase in the same message, contradicting AGENTS.md §3/§6 same-turn continuation; costs every Claude Code adopter one extra turn per task. Fix stub wording (+ audit sibling stubs for the same pattern; verify Codex/Antigravity adapter parity). From 2026-07-06 full audit (primary-verified) | framework | governance | P2 | — | quick-win | Shipped | — | PR #321 |
| 119 | Design Gate no-DSoT escape hatch — plan.md §Design Gate (+ engineering_guardrails.md §4.4) hard-fails `missing: [design_link]` on any user-visible UI plan; a tool-less adopter is hard-blocked. **Resolved v1.8.9 (R1)**: §4.4 already accepts a "URL **or file path**" artifact — the DSoT definition + both /plan stop messages now name a committed Markdown/ASCII wireframe file (`docs/design/<screen>.md`) as valid; gate stays HARD (no artifact still hard-stops). **The capability-seam `design_tool` escape (this row's original suggestion) was DESIGNED then roundtable-REJECTED (5 agents + 第十人 + 事前驗屍): it reincarnates the ADR-001-D2-rejected flag AND breaks `validate_downstream_capabilities.py`'s gate-relaxation-UNREPRESENTABLE allowlist — do NOT retry.** From 2026-07-06 full audit | framework | governance | P2 | — | quick-win | Shipped | — | PR #324 (v1.8.9) |
| 120 | Deploy-end enforcement onboarding (conversion wedge) — a default install ends with ZERO active machine enforcement: pre-commit hook is opt-in, deploy adds no CI workflow, and work logs are gitignored so the README "validate.sh reads the work trail" row can never fire in downstream CI. Add: hook-install prompt (or `--with-hooks` flag) at deploy end + printed CI/branch-protection recipe + a post-deploy self-check command the adopter can run immediately. Highest-leverage conversion fix from the 2026-07-06 audit; pairs with the standalone "secret gate in one command" story (#99) | dx | adoption | P1 | docs/specs/deploy-enforcement-onboarding.md | feature | Shipped | — | PR: branch feat/deploy-enforcement-onboarding |
| 142 | Enforcement reach-mechanism — surface "enforcement is OFF" to the disengaged adopter via a machine-backed nudge (the lever #120's surfacing deliberately does NOT reach: print only helps the adopter who reads deploy output). Candidate A: `validate.sh`/`.ps1` advisory when `core.hooksPath != .githooks` && not CI. Candidate B: `/bootstrap` core.hooksPath check + offer. Decide A-vs-B + enforce-vs-honest at its own plan — 第十人 confirmed the detection IS machine-observable (not honor-system theatre), but bootstrap-invocation + validate CI/hook context need light design. Split from #120 by user ruling 2026-07-21. | framework | adoption | P2 | — | quick-win | Pending | — | after #120 |
| 121 | README enforcement-matrix + 3-gems reframe — front-load the three immediately-usable wins (standalone credential gate; work-log + /handoff cross-session memory; 14 expert skill checklists) and add an honest enforced-vs-wire-up-vs-advisory matrix; current hero copy reads as default-on enforcement. CAUTION: README content is pinned in 2 layers (validate.sh/.ps1 encoding-canary phrases + tests/ci docs-pin pytest) — grep tests/ci + both validators BEFORE restructuring 2026-07-10 scope adds: (a) FAQ entry explaining the ⚡ ACX sentinel + gate blocks (currently zero user-facing explanation — README's only ACX hit is the .acx-incoming filename); (b) README_zh-TW.md:78 parity for the EN :82 caveat naming the 3 required checks + security-scans-not-required-by-default. | docs | adoption | P1 | — | quick-win | Pending | — | after #120 |
| 122 | Honor-system MUST deletion pass — per the [enforcement] Global Lesson ("every MUST = 1 hook OR validator OR test OR external observer; rules without enforcement should be DELETED"), sweep AGENTS.md + engineering_guardrails.md for MUSTs with no machine backing and delete, downgrade to guidance, or add the enforcement. Each deletion buys token headroom and restores credibility of the enforced set. One decision per PR where feasible. **Verified instance (2026-07-08 #119 roundtable): the §4.4 Design Gate is honor-system — ZERO validator checks `design_link` (confirmed). Enforce-vs-delete decision OPEN: (a) a T1 plan-lint FAILing a prod-UI plan that cites no `Design:` path (theatre→real enforcement, fits "machine-enforced not self-report"), OR (b) delete the gate (第十人's argument). CAUTION: enforcing ADDS adopter friction — weigh against the conversion thesis (#120/#121).** 2026-07-10 instances: lock acquisition is never verified anywhere (an agent that never calls recover_worklog_lock.py ensure is undetectable — TestGovernanceWiring is string-presence only); verify_agent_evidence.py stays deliberately unwired (CHANGELOG: dropped as vacuous) — treat as the documented exemplar of refuse-vacuous-wiring. | framework | governance | P2 | — | quick-win | Pending | — | — |
| 123 | Worked-example walkthrough — one real feature traced end-to-end (bootstrap→ship) with actual artifacts (spec, gate blocks, receipts, PR link) so a day-1 adopter sees the payoff without reading 49KB of bootstrap.md; link from README + INSTALL first-task section | docs | adoption | P3 | — | quick-win | Pending | — | after #121 |
| 124 | Stale P1 clusters revalidation — #1/#3/#7/#11/#13 (lifecycle engine, 2026-04-12) and #69/#77/#78/#79 (RPI-QRSPI + task capsule + skill harness) have ~3 months zero progress. 30-min evidence checkpoint per cluster: does the original pain still exist after the interim fixes (backlog archive split, token ratchet, #97 staleness WARN, worklog archival)? Resolve EVERY row to do-now / refine-to-precise / close-with-reopen-trigger — no silent carry 2026-07-10: month-3 lifecycle audit supplies the revalidation evidence — unbounded-accumulator pain confirmed live (Ship History 67, INDEX 99 lines, archive 1.3MB, backlog 60 rows, sidecar-per-release); cluster is NOT stale, re-prioritize at next pick. | research | governance | P2 | — | research | Pending | — | — |
| 125 | bootstrap §0a first-task interruption softening — a fresh deploy has an empty `docs/adr/`, so the adopter's very FIRST task is interrupted by the /app-init 5-question interview (Exit 2). Make it non-blocking: one-line recommendation + proceed, or fire only when the first architecture-change classification appears | framework | governance | P3 | — | quick-win | Pending | — | — |
| 126 | Claude stub guardrails-slim — 10 `.claude/commands` stubs listed `.agent/rules/engineering_guardrails.md` as an unconditional Required-read, contradicting CLAUDE.md step 4 / guardrails Quick-Skip Mode / bootstrap.md TOKEN LEAK BLOCK; minimal fix removes just that line (ask-local's §8.2 citation is functional, kept; the other stubs' Required-reads blocks stay — cosmetic only, closed) | framework | governance | P2 | — | quick-win | Shipped | — | PR #327 |
| 127 | SSoT caps enforcement + ship-history archival — current_state.md Ship History hit 67 entries vs ship.md:208 cap 10 (~26k tokens ≈81% of SSoT re-read every bootstrap); spec_index_max_entries:30 config key had NO validator consumer (dead cap). One-time archival to archive/ship-history-2026.md + new check_ssot_caps.py (WARN-tier, both validators, ADR-006 pattern) + config key ship_history_max_entries:10 + NOT-READY re-review remediation hint in both validators' gate parser (message-only; the illegal:plan->review FAIL was correct but cryptic) | framework | governance | P2 | — | quick-win | Shipped | — | PR #328 |
| 128 | Gemini adapter validator parity — validate.sh checks claude_required_files + codex rules but ZERO Gemini surfaces; GEMINI.md (15 lines, @imports AGENTS.md, deploys downstream as scaffold) can rot/vanish undetected. Fix: presence + content-pin (pin the stable `@AGENTS.md` import line) in both validators. EXPLICITLY CLOSED with reopen-trigger: building .gemini/commands dispatch tree or GEMINI_PLATFORM_GUIDE — speculative over-build, zero Gemini-adopter evidence (matches #85 Cursor precedent); reopen on first real Gemini adopter signal or a governance change needing per-command Gemini files | framework | governance | P3 | — | quick-win | Pending | — | — |
| 129 | AGENTS.md scaffold-tier ossification signal — AGENTS.md is preservation-tiered (deploy.sh scaffold): an adopter who edits it directly gets every upstream governance/safety fix silently diverted to AGENTS.md.acx-incoming forever, with no staleness signal; carries Core Directives + safety floor. Design needed: advisory drift signal (e.g. deploy-end notice or validator WARN when .acx-incoming sidecars linger), NOT a tiering change (ADR-005 settled). From 2026-07-10 month-3 lifecycle audit | framework | deploy | P2 | — | quick-win | Pending | — | — |
| 130 | knowledge_sources path-rot advisory — validate_downstream_capabilities.py checks path values are non-empty strings but never existence; a moved/deleted KB path silently degrades to UNREADABLE at bootstrap with no user-facing WARN. Add existence WARN (gate-safe additive; respect `${ACX_KB_PATH}` env resolution + BYO-no-manifest cases). From 2026-07-10 lifecycle audit | framework | governance | P3 | — | quick-win | Pending | — | — |
| 131 | Global Lessons all-HIGH deadlock escape — append_lesson.py hard-blocks at cap 20 AND --archive refuses HIGH entries; at 20/20 all-HIGH the learning loop deadlocks with no self-service escape (framework repo already at 10 HIGH of ~20). Policy decision needed: HIGH-demotion path with user sign-off vs cap bump vs HIGH-consolidation lesson-merge. From 2026-07-10 lifecycle audit | framework | governance | P2 | — | quick-win | Pending | — | — |
| 132 | Work-log cancel/abandon path — no /cancel workflow exists; abandoned tasks' frozen work logs accumulate until a manual archival chore (INDEX shows ~20 such backfill/stale-archival records). Design: a lightweight cancel disposition (archive with status: cancelled + INDEX entry, no ship receipts required). From 2026-07-10 lifecycle audit | framework | governance | P3 | — | feature | Pending | — | — |
| 133 | Draft-spec staleness WARN — specs sit in status:draft indefinitely with zero nag (routing_actions get a 14-day staleness WARN; drafts get none). Mirror the config-driven pattern (draft_spec_warn_days). From 2026-07-10 lifecycle audit | framework | governance | P3 | — | quick-win | Pending | — | — |
| 134 | Validator honesty pair — (a) templates/current_state.md ships `- **Active Backlog**: (none yet)` which matches NEITHER validate.sh:2197's bare-`none` FAIL branch nor the backtick path extractor → the intended FAIL is unreachable on template-default SSoTs (needs a design look: template value vs regex, avoid failing fresh deploys); (b) worklog template `## Evidence` ships a `>` guidance blockquote that survives body extraction so a placeholder-only Evidence section reads non-empty and passes the shipped-empty-Evidence check (validate.sh:1572-1574 / ps1:1465-1468). From 2026-07-10 doc-consistency audit | framework | tooling | P3 | — | quick-win | Pending | — | — |
| 135 | Quick-win-lite worklog template — the 17-section/13-field template is identical for a 5-line quick-win and an architecture change; a classification-aware lite variant would cut agent-borne ceremony ~50% but touches the validator's required-section sets + tests. Parked pending conversion-first thesis (#120/#121 outrank). From 2026-07-10 ceremony audit | framework | governance | P3 | — | quick-win | Pending | — | — |
| 136 | Gate-progression FAIL-count parity — bash's completeness check breaks at the first failure (1 FAIL contribution per malformed log) while ps1 falls through into the progression-order loop and double-counts (bash reports 2, ps1 reports 4 on the same 2 malformed fixtures). Verdicts agree, counts drift — cosmetic-but-confusing cross-platform divergence. Found during WP3 receipt-integrity verification (see PR #341 Work Log Known Risk) | framework | tooling | P3 | — | quick-win | Pending | — | — |
| 137 | Promote check_routing_actions.py downstream — the structural routing_actions check is source/CI-only; downstream deploys keep the weaker native backstop by deliberate WP1 decision. Promotion = deploy.sh/ps1 runtime_tools + manifest golden + the 2 deployed-fixture slow tests updated as a set (the #334 class of gap, this time tracked not accidental). From 2026-07-11 audit-wave WP1 (PR #340) | framework | tooling | P3 | — | quick-win | Pending | — | #113 |
| 138 | Decision-capture hardening — decide.md §5 "promote to ADR during /ship" is an untriggered orphan: ship.md never reads Work Log ## Decisions (its §7 reads only the spec's Domain Decisions, feature/arch only), zero validator/test/eval enforcement, worklog template lacks the section, quick-win/hotfix have no spec channel and the L2 nudge is user-gated (headless no-op). Evidence: 82/113 archived INDEX.jsonl entries carry decisions with empty specs[]; 3 of 4 live Ship History decision clusters unbacked. Fix shape: enforce-or-delete §5 + ship-time decision disposition (promote → ADR/L2 or mark local) + WARN-tier validator check behind the ADR-006 seam — DELETION-FUNDED (token headroom ≈0) + sh/ps1 parity. From 2026-07-16 decision-capture audit. Shipped 2026-07-16 (branch feat/decision-capture-hardening: ship.md 2b + tripwire, decide.md §5 rewrite, template §Decisions, check_decision_disposition.py Signal A+A2, bootstrap none-guard; spec decision-capture-hardening.md) | framework | governance | P2 | docs/specs/decision-capture-hardening.md | feature | Shipped | — | — |
| 141 | Archived-worklog Phase-Summary audit false-positives on non-worklog archive artifacts — `archive/global-lessons-archive.md` (first created by /retro chain-aware archival 2026-07-19) tripped the scan; `test_171_ship_history_no_phase_summary_warn` enforces zero such WARNs framework-wide, so the "defer" disposition was invalid and PR #353 merged red (the #270-class incident, operator-caused: unconditional `;` merge chain + subset-only local tests). Fixed same-day: exclusion filter extended to `global-lessons-archive*` in both validators (sh -iname + ps1 -notlike, parity). Residual: promote the filter to a documented non-worklog allowlist when a third artifact class appears. | framework | tooling | P3 | — | quick-win | Shipped | — | #140 |
| 140 | Validator parity: archived-worklog `## Phase Summary` heading match is PREFIX in validate.sh (awk `/^## Phase Summary/`) but EXACT-then-newline in validate.ps1 (regex `^## Phase Summary\s*\r?\n`, ps1:1996) — a suffixed heading (e.g. `## Phase Summary (full entries)`) PASSes sh but WARNs ps1, breaking `test_validator_count_parity_on_framework` (exposed 2026-07-19 by the /handoff §6 compaction-overflow file on PR #352; hotfixed by exact-heading rename in the overflow artifact). Fix = align both parsers to the same match rule (prefer prefix-or-exact, sh+ps1 in one change) + a parity fixture with a suffixed heading. | framework | tooling | P3 | — | quick-win | Pending | — | #136 |
| 139 | Durable homes for orphaned live-window decisions — (a) design_tool/ADR-011 unanimous rejection: amend ADR-001 (per its own 2026-06-11 amendment precedent) + a document-governance.log.md L2 entry; today it lives only in Ship History pos-7/10 (ejected in ~4 ships), Shipped-filtered #119 prose, and the one review snapshot lacking a routing_actions block; (b) same pass evaluates the ship-history-cap-10 rationale + NOT-READY FAIL-preservation decision and the point-in-time archival precedent. Soft deadline: before ~4 more ships. From 2026-07-16 decision-capture audit. Shipped 2026-07-16 (branch chore/orphan-decision-homes; instance-batch only — #138 systemic fix stays open) | framework | governance | P2 | — | quick-win | Shipped | — | #138 |
| 143 | Governance eval zero-coverage drift — 28 of 45 MUST-bearing sections carry ZERO guarding eval cases (primary-verified on main 2026-07-22; the set is byte-identical pre/post the #107 matcher tightening, so it is accumulated drift from ships adding MUST headings without cases, not a matcher artifact); correction from the wave-close validate run: the validators DO surface it as a WARN line (`governance eval coverage: 28 MUST-rule section(s) without eval cases`, tier-blind) — the real gap is WARN-numbness, not invisibility: never-blocking + tier-blind (mixes machine-enforced and principle-tier rules), it grew ~unwatched since ≥2026-07-10 ("governance-eval-coverage advisory" already in that wave's pre-existing-WARN list) — the inverse-direction sibling of the [eval-mapping] verifier-without-defense lesson. Decide at pick-up: backfill cases for load-bearing rules vs a cap-at-today zero-coverage-count ratchet test (mirror `test_directive_count_ratchet.py`) vs WARN-tier surfacing in validators. RE-CLASSIFY at pick-up by the chosen path: ratchet/WARN-shaping fits quick-win; a 28-case backfill does NOT (own bootstrap + spec). From the #107 implement disclosure (PR #358). **Coordinate with #165** (2026-08-11): that row's trigger-accuracy suite would backfill cases for `AGENTS.md §Skill Activation Triggers`, one of the 28 counted here — same WARN, two pickup paths. | framework | governance | P3 | — | quick-win | Pending | — | — |
| 144 | Python discovery startability probe — validators selected interpreters by existence only; the Windows WindowsApps python3 App-Execution-Alias stub (exists on PATH, exits 9009 with args) shadowed a working python, causing spurious python-check failures; fixed by a silent -c probe with python3→python fallthrough (pre-existing defect, surfaced by 2026-07-22 codex post-ship review, primary-verified). Shipped 2026-07-22 (branch fix/python-discovery-startability) | framework | tooling | P2 | — | quick-win | Shipped | — | — |
| 145 | Conflicting-directive scan across governance surfaces — ADR-011 audited whether each directive has *enforcement backing*, never whether directives *contradict each other*; that axis has never been swept. Two confirmed instances: (a) #126 (shipped) — 10 `.claude/commands` stubs listed guardrails as an unconditional Required-read, contradicting CLAUDE.md step 4 / TOKEN LEAK BLOCK, fixed case-by-case with no systematic scan; (b) found live 2026-07-25 during a bootstrap — `bootstrap.md §1` instructs a `Last Verified` SSoT write, but `AGENTS.md §vNext State Model` gives an **exhaustive** non-ship SSoT-write exception list (`/retro`, `/app-init`, `/adr`) that excludes `/bootstrap`; resolved only by the AGENTS.md-over-workflows precedence rule, i.e. silently, with no gate. Scope: 4 phase-entry surfaces × `.agent/workflows/*` × `.claude/commands` stubs. Externally motivated by Anthropic's 2026-07-24 context-engineering guidance (conflicting instructions are a named cause of degraded instruction-following) | framework | governance | P2 | — | feature | Shipped | — | — |
| 146 | Force UTF-8 decoding in test subprocess helpers — on a Windows box whose ANSI code page is not UTF-8 (cp950/Traditional Chinese), `subprocess.run(..., text=True)` decodes tool output with the system codec; a single UTF-8 em-dash (`0xe2`) raises `UnicodeDecodeError`, `stdout`/`stderr` come back `None`, and the assertion dies as `TypeError: argument of type 'NoneType' is not a container` — masking the real cause and making a clean tree look broken. Confirmed on clean main @ 1627852 (2026-07-25): `test_lesson_chain_archival.py` ×2, `test_verify_agent_evidence.py` ×3, `test_trigger_metadata_tools.py::test_command_sync_rejects_alias_directive_retarget` — 6 local FAILs, same commit green in CI. Fix: pass `encoding="utf-8"` (+ `errors=`) in those helpers. Documented meanwhile as repo-gotchas #13. **Shipped 2026-07-26 (branch fix/utf8-subprocess-decoding)** — AST scan found the defect was wider than "test helpers": 32 sites / 15 files, incl. 2 downstream-shipped tools (`lint_spec_drift.py`, `verify_agent_evidence.py`) that decode git filenames inside `/plan` and `/review`; all fixed + cap-at-zero AST ratchet `tests/ci/test_subprocess_encoding.py` | framework | tooling | P2 | — | quick-win | Shipped | — | — |
| 147 | `.codex/agents/*.toml` phase shims are deploy orphans — the five `acx-*` Codex agent definitions exist in-repo but `deploy.sh:1035` ships only `.codex/INSTALL.md`; the Claude twins ship at `core` tier (`deploy.sh:1019-1026`, golden `tests/ci/fixtures/deploy_manifest_golden.txt:105-109`) while the Codex files appear nowhere in the manifest golden. Any Codex-side agent change therefore reaches ZERO adopters, and the Claude/Codex adapter pair is silently non-equivalent downstream — a cross-platform-parity gap that no test currently detects (the shim checks at `validate.sh:257-261` / `validate.ps1:331-335` look only at `.claude/agents/`). Surfaced 2026-07-27 by the #78 roundtable, primary-verified. Decide at pick-up: deploy at `core` (BOTH deploy.sh whitelist spots + golden, per repo-gotchas) vs delete as dead weight — the decision needs one prior fact that is currently **UNVERIFIED**: whether Codex consumes `.codex/agents/*.toml` at all. | framework | tooling | P2 | — | quick-win | Pending | — | — |
| 148 | Hyphenated `Verdict:` tokens count as FORWARD gate progression in `validate.sh` and diverge from `validate.ps1` — `validate.sh:1518` captures the verdict with `([A-Za-z _]+?)`, a class containing no `-`, so a receipt spelled `Verdict: NOT-READY` (or any future hyphenated token) makes `re.search` return `None`; the guard at `:1519` is `if v and ...`, which then falls through to `gates.append(phase)`, recording an explicitly non-PASS receipt as a **passed gate**. `validate.ps1:1487` matches `Verdict:\s*PASS` positively and treats the same line as a reverse edge, so the two validators disagree on the same Work Log. Latent, not live: the documented spelling `NOT READY` is space-separated and parses correctly today — but this is a fail-OPEN default in the gate-progression parser, and the next change to verdict vocabulary walks straight into it (it would have silently re-created the exact implicit-pass defect #78 set out to fix). Fix: make the `v is None` branch fail-closed in both validators + a parity fixture carrying a hyphenated token. Surfaced 2026-07-27 by the #78 roundtable, primary-verified by reading the regex and its guard. | framework | tooling | P2 | — | quick-win | Pending | — | — |
| 149 | Work-log checks vanish instead of reporting SKIP when `.agentcortex/context/work/` is empty — roughly **20 result lines** (`all active work logs have Current Phase field`, `gate evidence phase progression is legal`, `security findings section present`, `loaded-sections receipt present`, `no review PASS receipts with unresolved UNPROVEN rows`, `no reclassification header inconsistency`, and the rest of the active-log family) emit **nothing** when there are no active logs, rather than a `SKIP` line. Measured 2026-07-27 on a single commit: clean `main` worktree `pass=99 warn=3`; the same tree with two active logs `pass=116 warn=4`; after archiving both, `pass=99 warn=3` again. Consequences: (a) a fresh clone or a downstream adopter with no active work log silently runs ~20 fewer governance checks and the top line still says "integrity check passed" — the same fail-open family as #148; (b) the run-to-run count becomes unusable as a regression signal, which is why isolating a delta needs stash-and-rerun rather than a clean worktree (documented as repo-gotchas #14). `validate.sh` already has the right precedent elsewhere (`acx phase shim skill check -- no acx-*.md shims found` emits a real SKIP). Fix: emit one SKIP per absent-input check family in **both** validators + a fixture with an empty `work/`. **Shipped 2026-07-27 (branch `fix/149-worklog-checks-skip-when-absent`)** — one family-level SKIP per validator (`active work-log checks -- no active work logs in .agentcortex/context/work/ (18 checks not applicable)`), guarded by `worklog_count -eq 0` / `$worklogs.Count -eq 0`, placed at the head of the guarded block. **The top-line qualification was considered and rejected**: #113's `reduced assurance` labels a *capability* gap (the interpreter is missing, so checks that exist cannot run), whereas here the *input* is absent — a repo with no in-flight work is fully checked with respect to what exists, and permanently labeling every clean install "reduced assurance" would be noise. The `skip=N` counter carries the signal instead. ADR-006 cost: **+1 native site per validator** (baseline 201→202 / 202→203) with a justification entry — family-level, not per-check, because the wrappers map exit!=0 → FAIL and cannot express SKIP, and a Python tool would have to re-implement the whole family to know the count. Pinned by `tests/ci/test_validator_worklog_family_skip.py` (4 structural incl. a guard-direction check and a per-check-expansion guard + 1 behavioral red/green over a real `deploy.sh` install); proven red 4-failed → green 5-passed. Incidental parity fact confirmed while writing it: both validators deliberately exclude dotfiles, so the shipped `.gitkeep.md` placeholder does not count as a log — Python's `pathlib.glob` does not share that behavior. | framework | tooling | P2 | — | quick-win | Shipped | — | — |
| 150 | Scope-signal matching misses plural/singular inflections, so natural phrasing fails to activate Skills — `trigger_runtime_core.values_match` compares **token sets** after `normalize_text`, so `tokens` and `token` share no token and never match. Measured 2026-07-29 against the real registry by inflecting only the head word of each signal: **53 inflections across 12 of 14 Skills stop matching**, of which roughly **26 are phrasings a person would actually type** (the remainder are mechanical artifacts like `loggings`, `core logics`, `form handlings`). Worst hit is **`auth-security`, whose entire signal set is singular** — `logins`, `passwords`, `tokens`, `sessions`, `roles`, `permissions` all miss, and "roles and permissions" or "handling tokens" is the natural way to describe that work; the Skill that silently fails to load is the security one. Also affected: `database-design` (`migrations`, `schemas`, `tables`, `indexes`), `api-design` (`api endpoints`, `routes`, `controllers`), `doc-lookup` (`framework APIs`, `config options`, `CLI commands`), `red-team-adversarial` (`permissions`, `boundaries`, `dependencies`), `production-readiness`, `frontend-patterns`, `systematic-debugging` (`bugs`). Two entries suffer the inverse because the registry stores them plural — `4+ files`, `parallelizable modules` miss the singular. **Pre-existing and unrelated to PR #379**: the old `resolve_runtime_contract.py` carried its own bidirectional-substring matcher which masked the gap, so the simulation CLI reported activations the runtime never performed; #379's delegation to the canonical core removed the mask and made the runtime's real behavior observable. That is the delegation working, not a regression. `values_match` backs all three signal families (`manual`, `scope`, `failure` — `trigger_runtime_core.py:909-911`), so any change moves Skill loading for every user. Options, in ascending risk: (a) widen `trigger-registry.yaml` data with plural forms — zero code risk, per-row reviewable, whack-a-mole and the registry grows; (b) add singular/plural normalization inside the token comparison — one fix for all three families, but must not mangle `process`/`class`/`analysis`, so it needs a real corpus test; (c) accept and document. Not a candidate: restoring bidirectional substring, which over-matched (`handling form` activated `frontend-patterns`). | framework | skill-ecosystem | P2 | — | quick-win | Pending | — | — |
| 151 | A downstream adopter cannot verify that a generated Skill is discoverable — ADR-005 preserves customized `.agents/skills/*/SKILL.md` byte-for-byte, so an adopter who ran `/app-init` before PR #379 keeps the comment-first shape that native hosts cannot parse; the compatible source arrives only as an `.acx-incoming` sidecar that a human must merge. That preservation is deliberate and declared (`docs/specs/skill-runtime-modernization.md`). What is missing is the closing half: **nothing deployed can tell the adopter their Skill is invisible.** `check_skill_provenance.py` is source-repo-only and is not in `deploy.sh`'s runtime-tools whitelist (correctly — it self-skips whenever `.agentcortex-manifest` is present, so shipping it would install a permanent no-op); deployed `validate.sh` therefore records `skill provenance + compatibility floor -- tool not present`, and the skill checks that do run only confirm a stub and a `SKILL.md` exist, never that frontmatter parses. Net effect: run `/app-init`, generate a non-discoverable Skill, run `validate.sh`, and be told "integrity check passed". The population selected against is precisely the one that used the framework as intended — an adopter who never customized anything gets the fix for free. PR #379 removed the false assurance from `.agent/workflows/app-init.md` (it had claimed downstream generation stays "validator-verifiable") and now states plainly that nothing in the installed tree checks the contract, which is honest but not a mechanism. Candidate mechanisms, each with a real cost: deploy a stripped frontmatter-only checker (reopens the no-op question and adds a maintained downstream artifact); have `deploy.sh` run one non-blocking frontmatter scan at the end of an upgrade and print what it found (no new deployed tool, but deploy gains a responsibility it does not have today); or extend the `.acx-incoming` notice to say *why* the sidecar matters rather than only that the file was skipped (cheapest, reaches only adopters who read deploy output). Needs a scope decision before any of these — do not pick one by default. | framework | adoption | P2 | — | feature | Pending | — | — |
| 152 | Six always-applicable `/ship` rules are indented as children of `- **No-Python fallback**` — `.agent/workflows/ship.md:183` sits at column 0 and `:184-186` / `:194-196` sit at 3-space indent, so by markdown structure the Spec Index Cap, the Ship History **newest-first** rule (including its "do NOT use `--mode append`" warning), the 10-entry rotation, and the relative-link depth hazard all read as sub-items of a no-Python condition. An agent that has Python can legitimately conclude the whole block is N/A and skip it — and this repo has already been bitten once by Ship History ordering. Pre-existing on `main` since the block was authored; found during PR #381 but deliberately excluded from it because the fix is not pure whitespace: de-nesting `:186` ("Use the format:") moves the indent-5 code fence at `:188-192` with it, so the block needs restructuring plus a re-read of what `test_ssot_heartbeat_contract.py` pins on `:185`. Verify against `git show main:.agent/workflows/ship.md` before assuming any current line number. | framework | governance | P2 | — | quick-win | Shipped | — | — |
| 153 | The archive non-Work-Log exclusion was only ever applied to **one of four** scanners per platform. `validate.sh:2166` carries `-not -iname 'ship-history-*' -not -iname 'global-lessons-archive*'` (added by #171 and #141 respectively), but `:2194` (M7 gate completeness), `:2222` (receipt schema) and `:2282` (M8 relative links) filter only `.gitkeep*`; `validate.ps1:2016` vs `:2047/:2078/:2115` mirror the same split. So `ship-history-2026.md` (205 KB) and `global-lessons-archive.md` are still swept by three checks that apply a complete-Work-Log contract to them; they pass today only because they happen to contain no `Gate:` lines. `_product-backlog.md` #3 already records the standing disposition — promote the filter to a documented non-worklog allowlist when a third artifact class appears. Doing it now, before that third class exists, is the cheap moment. Note `check_decision_disposition.py:274` got this right (non-recursive `glob`) and is pinned by `tests/guard/test_decision_disposition_check.py:520`. | framework | governance | P2 | — | quick-win | Pending | — | — |
| 154 | `raw_intake_cleanup: must_delete` (`.agent/config.yaml`) has **zero consumers repo-wide** — a dead key — and the check that nominally serves it disagrees with its own documentation: `validate.sh:2663` / `validate.ps1:2533` count **every** `_raw-intake*.md` with no backlog-status guard, while `spec-intake.md:61` documents deletion only once all features are Shipped/Cancelled. A legitimately mid-flight intake — the exact file `spec-intake.md:39` instructs the agent to create — therefore WARNs on every run until ship deletes it, which is WARN-numbness by construction. Resolve in one direction: either implement the documented guard in both validators, or delete the dead key and restate the check as "any raw intake present at ship time". Do not leave both halves. | framework | governance | P3 | — | quick-win | Pending | — | — |
| 155 | `/handoff §6` compaction moves a log from `.agentcortex/context/work/` to `.agentcortex/context/archive/work/` — a depth 3→4 shift — with no relative-link warning, while `ship.md:196` carries an explicit one for the *shallower* depth 2→3 move it performs. `handoff.md:149` covers the Phase-Summary consequence correctly; only the link hazard is unmentioned. M8 (`validate.sh:2282`, `-maxdepth 2`) does reach `archive/work/`, so the WARN is reachable; it has never fired only because none of the 12 files currently there contains a relative link. One sentence mirroring `ship.md:196`. | framework | governance | P3 | — | tiny-fix | Pending | — | — |
| 156 | Fenced-decoy footgun in the new `## Spec Index Archive` parsing (PR #381): both `validate.sh:2377` and `validate.ps1:2237` bind to the **first** line matching `^## Spec Index Archive`, so a fenced example containing that string at column 0 earlier in `current_state.md` shadows the real section and the genuine archived entries become invisible → `not indexed` FAIL. Both platforms agree, so this is a footgun rather than a parity bug, and it surfaces loudly rather than silently. Reachable in practice because `current_state.md` Ship History routinely embeds long technical prose and fenced examples. Options: bind to the last match, require the archive to be the final `##` section, or strip fenced regions before scanning. Until fixed, ship records must not write that string at column 0. | framework | governance | P3 | — | quick-win | Pending | — | — |
| 157 | The `tiny-fix` skip in the archived-Work-Log Phase-Summary audit is **effectively dead on Windows**: `validate.sh:2154` matches `^- \*?\*?Classification\*?\*?:` (bold optional) and strips backticks, while `validate.ps1:2021` requires literal `**Classification**` and does not strip backticks. This repo's archived logs use `- Classification: \`architecture-change\``, i.e. unbolded and backticked, so the PowerShell branch never recognises a classification and never skips a `tiny-fix` log. The two platforms are therefore auditing different file sets today. Cosmetic sibling in the same three sites: `-not -name '.gitkeep*'` is case-sensitive in bash while `-notlike '.gitkeep*'` is case-insensitive in PowerShell. Cross-platform parity is mandatory in this repo, so the sh form is the reference. | framework | governance | P3 | — | quick-win | Pending | — | — |
| 158 | Verification look-timing: quoted completion evidence may predate the last Work Log write — no binding surface requires the quoted validator/test run to postdate the final write; `implement.md:18` (receipt append) and `:149-150` (5-Gate) are mutually unordered, and `implement.md:170-172` explicitly orders a Work Log write (Phase Summary line, validator-inspected at `validate.ps1:1579`) AFTER evidence capture. Sim-evidenced 2026-08-08 + tenth-man-hardened (`docs/reviews/2026-08-08-govern-audit-task-simulation.md` F1): S1 quoted `pass=99 warn=3 fail=0 skip=3` — forensically the work-log-ABSENT fingerprint, so the run predates the log's existence — while the true state was `fail=1` (missing plan receipt = illegal `bootstrap→implement` at every tier map, both platforms; the FAIL is correct validator behavior, refuter-verified). Same class at ship level twice before (v1.8.18 pre-archival "117 PASS" erratum; 2026-07-27 archive-then-test lesson). Detection already LIVE — the whole gap is look-timing. Fix shape (pre-mortemed): ONE lowercase honest-labeled sentence in `shared-contracts.md` §5-Gate item 3 ("evidence quoted as final comes from runs after the last write, including Work Log writes") — shared-contracts has 0 instrument cost (see #163) and reaches all phases; optional ≤256-char ordering clarification in `implement.md` (×12 multiplier, 771-token total headroom); directive ratchets at zero headroom (case-sensitive — no new MUST). REJECTED at tenth-man: a post-append self-check tool (duplicates the live detector, #78-refutation ground, ADR-006+deploy cost, and reproduces unobserved-signal one level up). **Shipped 2026-08-08 (PR #388 + review commit `bec66d4` terminal-write exception).** | framework | governance | P2 | — | quick-win | Shipped | — | #163 |
| 159 | Quick-win Work-Log requirement is unreachable at decision time — the only unambiguous statement, `engineering_guardrails.md:343` "Work Log MUST be created (lightweight…)", sits in a file `bootstrap.md:31` TOKEN LEAK BLOCK forbids tiny-fix/quick-win from reading (sole exemption §13); the surfaces quick-win IS shown omit it by asymmetric drafting (`bootstrap.md:205` "Plan → Execute → Evidence" has no Work-Log clause while `:209-210` feature/arch lines do; `state_machine.md:59` names evidence only); and `ship.md:64` "missing receipts are a WARN" both supplies citable lenient cover AND understates live enforcement — `validate.ps1:1516-1518` sets quick-win requiredPhases=(bootstrap,plan,implement) with misses in the FAIL-tier counter (doc-wrong-in-lenient-direction, PR #367 class reversed). Sim-evidenced 2026-08-08 + tenth-man-reshaped (report F2): S2 skipped the log citing `:64`, S4 skipped classification+log silently — both finished byte-identical to clean baseline (`pass=99 skip=3`, refuter-re-run); no dirty-tree/no-log detection exists in either validator (`cur_key` has no reader outside the per-worklog loop). Fix shape: reach the requirement to the loaded surfaces (one clause each at `bootstrap.md:205` + `state_machine.md:59`) + align `ship.md:64` UP to the validator's actual tier; measure against the 771-token headroom + case-sensitive directive ratchets. BLOCKED at tenth-man: any "no-log WARN" (classification lives inside the absent file — the validator cannot tell legal-tiny-fix-no-log from illegal-quick-win-no-log; the #114 wall; dirty-tree predicate is CI-invisible, commits-ahead is constant-true). **Shipped 2026-08-08 (PR #388 — textual reach + upward alignment only; the blocked detector stays blocked).** | framework | governance | P2 | — | quick-win | Shipped | — | #114, #149, #163 |
| 160 | `generate_compact_index.py:42` writes CRLF on Windows (`write_text` with no `newline=`) — regenerating `trigger-compact-index.json` (the mandatory companion step after editing a detail_ref doc) emits 475/475 CRLF lines. Tenth-man-corrected blast radius: `.gitattributes:16` `*.json text eol=lf` filters the bytes at the boundary (`git hash-object` with attributes = identical blob, `git diff` empty, `validate.ps1` byte-identical to baseline — the freshness `--check` reads universal-newlines and is newline-blind by construction), so actual impact is a spurious ` M` in `git status` plus the hand-fix effort agents burn (sim S1 did). Fix: `newline="\n"` at the write site + sweep sibling `write_text`/`open` writers in `.agentcortex/tools/`. Test traps (refuter-verified): the real home is `.agentcortex/tests/test_trigger_metadata_tools.py` (NOT `tests/ci/`); an LF assertion on the committed file is vacuous (git checks out LF regardless); the bug does not manifest on Linux (`os.linesep`), so assert bytes of a fresh generator run into a tmp path, and note the Windows pytest shard is heavy-gated + non-required. **Shipped 2026-08-08 (PR #389 + review commit `c1f724b` append/archive byte tests; sweep also fixed 4 append_lesson sites and surfaced #164).** | framework | tooling | P3 | — | quick-win | Shipped | — | — |
| 161 | Work-Log `## External References` referents are never existence-checked — the template section (`templates/worklog.md:81`) appears zero times in either validator; a log citing a nonexistent spec path and a nonexistent PR URL passes everything (probe P1a, 2026-08-08; originally mis-deduplicated into #122/`[enforcement]`, refuted at tenth-man — those cover self-attested *claims*, not mechanically-checkable *referents*). SHA header fields are already value-checked as commits (`validate.sh:1338` rev-parse) but nothing inspects range content — that half stays closed on cost. The needed primitive already exists for another field: `validate.sh:2452-2453` FAILs on a missing backlog ref file. Fix: WARN-tier spec-path existence check (Spec rows only; `—` and URLs exempt; PR column = format-presence only, no network) — new-check rules apply (ADR-006: Python tool behind run_python_check/Invoke-PythonCheck or extend an existing worklog Python tool; sh↔ps1 parity; deploy whitelist ×2 + manifest golden only if shipped downstream). **Shipped 2026-08-08 (PR #391, seam-only/source-only + review commit `691ea68` fence-aware parser; counted-WARN promotion stays #103(d)).** | framework | tooling | P3 | — | quick-win | Shipped | — | — |
| 162 | Lesson-chain tail mangle is permanently cemented, not self-healing — reopened from the 2026-08-08 audit's refuted F5 close (experiment-verified): a format-mangled tail bullet (e.g. missing `[Severity:]`) is skipped by `check_lesson_chain.py`'s strict LESSON_RE while mid-chain mangles ARE caught via the successor's `[prev:]` mismatch (fail-closed); but the NEXT `append_lesson.py` append anchors its `[prev:]` to the strict parser's `lessons[-1]` (`:56`,`:108`,`:119-120`) — i.e. PAST the mangled bullet — after which the chain re-verifies `intact` forever with the mangled lesson still physically present. Aggravators: cap gate (`:109`, strict count) vs section bounds (`:90`, loose `- [Category:` prefix) divergence lets a mangled tail unblock an append the 20-cap should refuse; the only signal observed in the tampered state was a MISLEADING over-cap WARN (at-cap repos only; below cap = fully silent). Entry path is hand-edits only. Fix: strict/loose count-parity guard in `append_lesson.py` (refuse append when loose≠strict, point to `check_lesson_chain.py`) + consider a chain-checker WARN for prefix-matching-but-unparseable bullets; red/green via `test_lesson_chain_archival.py` extension. **Shipped 2026-08-08 (PR #390, rebased over #389; fail-closed both sides).** | framework | tooling | P3 | — | quick-win | Shipped | — | — |
| 163 | Lifecycle token instrument omits `shared-contracts.md` — `analyze_token_lifecycle.py:29-38` `PHASE_WORKFLOW_MAP` maps only the 8 phase files, so a doc that `AGENTS.md §Shared Phase Contracts` mandates loading at EVERY non-tiny-fix phase entry carries **zero weight** in the 355k ceiling (empirically probed: +400 chars → aggregate delta 0, while `implement.md` = ×12 and `ship.md` = ×6). Two consequences: the ceiling systematically undercounts real per-task load, and the gap is a "free hosting" loophole — text can migrate to shared-contracts to dodge the ratchet while still costing real runtime tokens ~6×/task (the 2026-08-08 tenth-man pre-mortem explicitly reached for this loophole; #158's one-sentence fix knowingly uses it and depends on this row for honest accounting). Decide one way: add `shared-contracts.md` to the map with its true multiplier (≈ count of non-tiny-fix phase entries; instantly consumes the 771-token headroom → forces a deletion-funding pass) OR record the exclusion as deliberate with rationale in `token-governance.md`. Do not leave the silent middle. **Shipped 2026-08-08 (PR #395, decision D-1 = B+: deliberate exclusion documented in `token-governance.md §5.1` + `test_shared_contracts_size_ratchet.py` cap 7306; true-multiplier fold rejected on measurement — 10,362 tokens vs 771 headroom).** | framework | governance | P2 | — | quick-win | Shipped | — | — |
| 164 | `update_lifecycle_baseline.py:78` calls `Path.write_text(..., newline="\n")` — the `newline` parameter exists only on Python ≥3.10, so on the repo's declared 3.9 CI floor the call raises `TypeError`; CI-invisible because `validate.sh` exercises only `--dry-run` while the crash sits on the `--init`/`--apply` paths. Surfaced by the 2026-08-08 #160 fix sweep (agent-verified against docs.python.org, primary-verified by source read). Fix: switch to `path.open("w", encoding="utf-8", newline="\n")` (the #160 pattern) + decide whether any 3.9-floor test should pin the API surface (an AST scan for `write_text(...newline=` mirrors the `test_subprocess_encoding.py` cap-at-zero pattern). **Shipped 2026-08-08 (PR #395: tool site + 18 accumulated test sites [AST scope-setting grew 1→19, the #146 pattern] + `test_write_text_newline_ratchet.py` cap-at-zero incl. the `write_text(<bytes>)` accident-shape check a tenth-man pass added after catching a half-converted-opener regression pre-merge).** | framework | tooling | P3 | — | tiny-fix | Shipped | — | #160 |
| 165 | Skill trigger-accuracy eval suite — the unblocked half of #79, split out 2026-08-11 so it is not held behind #77/#78. **Scope of the gap (corrected by a refute pass before merge — the first draft overstated it):** activation *resolution* is already covered — `trigger_runtime_core.py` implements `values_match:860` / `skill_is_candidate:884` / `skill_is_activated:918` and `.agentcortex/tests/test_lifecycle_skill_activation.py` collects **53** tests over classification/phase gating, load-policy correctness, manual-activation blocking, and candidate/triggered goldens. What is genuinely uncovered is **free-text prompt → `intent_patterns` matching** (`values_match` is only ever fed explicit `manual_skills` lists, never natural language) plus **near-miss negatives**. Do NOT build a second resolver beside `trigger_runtime_core.py`. Motivation: `run_governance_eval.py --coverage` (executed, not read) lists `AGENTS.md §Skill Activation Triggers` among 28 zero-coverage MUST-rules of 45 — but that metric is the tier-blind one **row #143 already owns**, and #143 lists "backfill cases for load-bearing rules" as a pickup path, so coordinate with it rather than clearing the same WARN twice. Contract source: `.agentcortex/metadata/trigger-registry.yaml` — `detect_by.intent_patterns` and `detect_by.classification` are nested under `detect_by:`, while `phase_scope`, `load_policy` and `block_if_missed` are entry-level siblings (see `test-driven-development` at `:71-97`). Registry has 16 entries but exactly **14 `kind: skill`** (the other two are `kind: workflow` / `kind: policy`) — scope the suite to the 14. **Decide the scoring mechanism FIRST, it is not settled:** three entries carry `intent_patterns: []` (`:17`, `:43`, `:110`) and `:110` is `verification-before-completion`, a real skill with `block_if_missed: true` whose activation runs off `phase_conditions: [completion-claim]`, not prompt text — so "near-miss paraphrase" is undefined for them and they need a different negative shape (or explicit exclusion). Likewise `_score_case:136` scores a **transcript** and has no `expect_activation` branch, and `_run_agent:197` is a **live-model subprocess** — a static prompt-vs-pattern matcher is deterministic but risks testing a matcher written for the eval itself, while a live-agent run is stochastic and cannot carry a hard non-zero exit without the repeated-run aggregation that stays in #79. Deliverable: separate data file `.agentcortex/eval/skills.yaml` (positives + held-out negatives) + `run_skill_eval.py`, WITHOUT absorbing the governance eval (#79 scope guard; re-run it unchanged as the refactor's regression proof) + a run-identity JSON header (commit SHA, `resolve_skill_lockfile.py` snapshot digest, caller-supplied model/harness, explicit `unknown` where unavailable). Naming discipline: this measures trigger ACCURACY, not effectiveness — landing it does NOT close #79. Traps: a new validator check must go through run_python_check/Invoke-PythonCheck per ADR-006 (raw record_result/Add-Result fails the native ratchet); a downstream-shipped **tool** needs `.agentcortex/bin/deploy.sh` whitelist ×2 (`:739`, and `:946` = the member line inside the array opened at `:928`) + `tests/ci/fixtures/deploy_manifest_golden.txt` — note `governance.yaml` itself ships in neither, so the precedent for the **data file** is already set; prefer on-demand invocation over a second standing coverage WARN. | framework | skills | P2 | — | feature | Pending | [#398](https://github.com/KbWen/agentic-os/issues/398) | — |
| 166 | TruffleHog AC-5 SHA pin does not bind the scanning binary — `security.yml:101` pins the *wrapper action* at a 40-hex SHA and `tests/ci/test_security_workflow.py:211` (`test_ac5_trufflehog_version_pinned`) machine-enforces that pin on the `ci-security-scanning.md:25` grounds that "semver tags are mutable and do not provide supply-chain immutability" — but the step passes only `extra_args: --only-verified`, and the wrapper's own `action.yml` at the then-pinned SHA `00155c9d` declares `version: default: "latest"` (`:21-23`), binds it to `VERSION: ${{ inputs.version }}` (`:39`), and interpolates that into the `docker run` image reference (`:97`). **So the enforced control does not bind the artifact it was written to protect.** Verified two ways: source read of the wrapper at the pinned SHA (above), and a **`main`-branch** run from before the #386 bump — run `31288803917` at commit `44b2e33` (2026-08-09), which loaded the **old** action `trufflesecurity/trufflehog@00155c9d…` and then logged `version: latest` → `latest: Pulling from trufflesecurity/trufflehog` → `Digest: sha256:aa821cf4…` → `"trufflehog_version": "3.96.0"`. So `main`, pinned to the v3.95.8 action, was executing a **3.96.0** scanner pulled from a mutable tag. *(An earlier draft of this row cited PR #386's own job log instead; an external review correctly rejected that — #386's CI runs on the bumped head, so it shows the NEW wrapper pulling `latest` and proves nothing about the old one. The claim was re-evidenced from a pre-bump `main` run rather than withdrawn.)* **Severity raised P2 → P1 on external review, adopted:** the first draft priced this as bounded hardening, but AC-5's declared object *is* the scanner version ("All three scanner versions are pinned … TruffleHog via GitHub Action commit SHA"), so this is a supply-chain immutability control that does not hold — and the pre-bump `main` run above shows the executed image drifting under a pin that claims otherwise. The blast-radius argument bounds the *consequence*, not the control failure: the wrapper runs `--since-commit $BASE --branch $HEAD` (`action.yml:99,101`) so a detector change cannot retro-fire on history, and `Secret Detection (TruffleHog)` is not in the three branch-protection-required contexts so a red run cannot block a merge. Fix: add `version: "<pinned>"` (or an image digest) to the `with:` block at `security.yml:102-103`, and decide whether `test_ac5_trufflehog_version_pinned` should additionally assert the `version:` input — otherwise the test keeps certifying a pin that isn't one. **Drift trap the fixer must handle** (raised on independent review): a `with: version:` value sits *outside* dependabot's action-SHA bump surface, so after the next wrapper bump the pinned action SHA and the pinned image version will silently diverge unless the fix also covers how that value gets updated — pinning by image digest, or a test asserting the two agree. **Second, same-class defect in the same spec, found by the refute pass on this row and folded in rather than filed separately** (one fix touches both): AC-3 states the job "performs a full-history scan (`fetch-depth: 0`)", but the wrapper's incremental `--since-commit`/`--branch` invocation means it never does — and `test_ac3_checkout_full_depth` certifies only the `fetch-depth` value, never that a full-history scan occurs. That is the identical shape as the AC-5 half: a test certifying a property the workflow does not have. Whoever fixes this row should correct AC-3's wording (or make the scan actually full-history) in the same change. Surfaced 2026-08-12 during the dependabot backlog clear-out (#386 review); backlog-only per the issue-exposure policy (hardening of an existing gate, not feature-like). **Shipped 2026-08-12 (branch `fix/166-trufflehog-scanner-pin`)**: the scanner is pinned **by image digest** — `image` ends in `@sha256` and `version` carries the 64-hex manifest digest, so the wrapper's `"${IMAGE}:${VERSION}"` join composes a content-addressed reference, and `test_ac5_trufflehog_scanner_pinned_by_digest` asserts that form. A first design pinning an exact release tag with a comment-equality guard was **withdrawn before merge** after independent review showed a tag is still mutable and that the equality guard survived a mutation swapping the wrapper SHA. AC-3 reworded and AC-5 extended; the source `[DECISION]` whose rationale generated the false full-history claim was corrected in situ rather than deleted, and both decisions consolidated into the new `docs/architecture/ci-security.log.md`. Red/green proven with discrimination: removing the pin fails 2 tests, drifting the version alone fails only the equality guard. | framework | security | P1 | — | quick-win | Shipped | — | — |
| 167 | `validate.sh:2294` prints the broken-relative-link diagnostic with `printf '%b' "$archive_broken_link_list"`, and `%b` expands backslash escapes **in the data**, not just the `\n` separators the code embedded. The list carries an absolute path from the link checker, so on any Windows checkout under `C:\Users\…` the run emits `validate.sh: line 2294: printf: missing unicode digit for \U` to stderr and prints a mangled filename (`\a` in `\antigravity` becomes a bell, etc.) — e.g. `C:\Users\wen\.gemini\antigravity\scratch\agentic-os` rendered as `C:\Users\wen\.gemini` + BEL + `ntigravity\scratch` + BEL + `gentic-os`. **The diagnostic fails exactly when it is needed**: the WARN tells you a file has a broken link and then destroys the filename. Reproduced both directions locally in the same shell: `printf '%b'` on a `C:\Users\…` string errors and mangles, `printf '%s'` renders it intact. Fix: `%s` with the separators emitted differently (e.g. accumulate with real newlines and use `printf '%s'`), or sanitise to a `${path#$ROOT/}` repo-relative form before accumulating — the sibling list-dumps already do the latter, which is why they are unaffected. Scope check before claiming a family: 11 `printf '%b'` sites exist in `validate.sh`; the other 10 dump `${wl#$ROOT/}`-style forward-slash relative paths and are **not** currently reachable by this bug, so this row is one confirmed site plus a note that the pattern is fragile — do not "fix" the other 10 without a reproduction. `validate.ps1` parity to be checked at implement. Found 2026-08-12 while diagnosing a real WARN this repo's own archival raised. | framework | tooling | P3 | — | quick-win | Pending | — | — |
| 168 | `append_chain_entry.py` writes **CRLF on Windows** into `INDEX.jsonl` — the hash-chained audit log — and is an un-swept sibling of the #160 LF-stable-writers fix (which covered `generate_compact_index.py` and the four `append_lesson.py` sites but not this tool). Non-obvious because the code *looks* binary-safe: `append_chained` builds `json.dumps(...) + "\n"` and calls `os.write(fd, line.encode("utf-8"))` (`:89`, `:95`). The trap is `:92-93` — the flag set passed to `os.open` (write-only, create, append) omits `O_BINARY`, and on Windows `os.open` defaults to **text mode**, so `os.write` still translates `\n` → `\r\n`. **Reproduced from clean inputs**, not inferred: seeded a scratch `.jsonl` with a single LF line, appended `{"log":"b.md"}` inline (no CR anywhere in the input), got `b'…"prev_sha": "49cbf4ef"}\r\n'`. Real-world hit the same day — two entries appended during the #401 archival landed as CRLF in an otherwise all-LF 151-line file, leaving it `w/mixed` (`git ls-files --eol`) and requiring a manual byte-level normalise before commit; that hand-fix cost is exactly the harm #160 recorded. Chain integrity is NOT affected (`chain_sha` hashes the parsed object, not the raw line; `check_audit_chain.py` reported intact both before and after normalising) and `.gitattributes` `text=auto` normalises the committed blob — so this is hygiene plus agent-toil, same honest-small blast radius as #160, hence P3. **`O_BINARY` alone is NOT sufficient — external review caught this, adopted.** `.gitattributes` covers `*.json` (`:16`) but has no `*.jsonl` rule, so `INDEX.jsonl` falls under the blanket `* text=auto` (`:2`); with `core.autocrlf=true` a normal Windows checkout materialises the file as **CRLF**, and an `O_BINARY` writer would then append a lone `LF` to it — re-creating mixed EOL from the other direction. The `w/lf` state observed in the authoring session was a post-normalise artifact of that one working copy, **not a repository invariant**. Fix therefore needs both halves: (a) `getattr(os, "O_BINARY", 0)` in the flags at `:92-93`, and (b) an LF checkout contract — add `*.jsonl text eol=lf` to `.gitattributes` (matching the existing `*.json` rule) or document an explicit whole-file normalisation strategy. Pin with a byte-level tmp-emit test mirroring the ones #160 added. `migrate()` at `:143` needs the same treatment — `tmp.open("w", encoding="utf-8")` has no `newline=""`; the external reviewer independently reproduced **both** writer paths (append emits 1 CRLF, migrate rewrites all lines as CRLF), so that half is now evidenced rather than suspected. | framework | tooling | P3 | — | quick-win | Pending | — | #160 |
| 169 | `check_decision_disposition.py` is fail-open against off-spec Decisions syntax — `D_ENTRY_RE = re.compile(r"^### D-")` (`:80`) matches only canonical `### D-N:` headings, so a `## Decisions` section written as `- **D-N: …**` bullets yields zero entries and the caller's `if not entries: continue` (`:285-288`) skips the log entirely. The check then prints `decision disposition OK` for a log carrying ten undispositioned decisions. **Demonstrated by this repo's own #401**: the archived log `chore-archive-codex-review-log-20260812.md` used bullet form, `rg '^### D-'` found zero entries against ten `- **D-` bullets, and the checker reported OK; the defect was surfaced by external review, not by the tripwire that exists to catch it. This is the #138 Signal-A tripwire failing in the direction it was built to prevent — an agent that formats the section wrongly gets a green check instead of a WARN, which is worse than no check because it reads as compliance. Fix options, decide at implement: (a) WARN when a `## Decisions` section is non-empty, is not `none`, and contains zero canonical entries (detects the shape without guessing intent), or (b) additionally recognise `- **D-N` as an entry form. Prefer (a) — it keeps one canonical syntax and flags deviation rather than blessing a second dialect. Note the same section-vs-entry asymmetry is already documented at `:164` as deliberate for a different case, so read that comment before changing the parser. Add a red/green fixture with a bullet-form log. | framework | governance | P2 | — | quick-win | Pending | — | #138 |
| 170 | **Owner decision required** — five archived Work Logs publish a personal email address in the `Owner` header field, against the template's `Owner: <session-id or username>` (`worklog.md:16`): `arch-kb-seam-hardening-20260621.md`, `chore-v1.8.0-release-20260621.md`, `docs-readme-surface-kb-seam-20260620.md`, `chore-backlog-165-skill-trigger-eval-20260811.md`, and one further file (`grep -rl 'luvseldom@' .agentcortex/context/archive/` → 5). Surfaced during #401, where the same defect was caught in a *new* log and fixed before merge; these five predate it and are already committed to a public repository. This row exists because "surfaced to the maintainer" is not a tracked disposition under the no-deferred-disposition rule — the finding otherwise lives only in a commit message and a PR thread, neither of which is a pickup surface. **Not an AI decision**: archives are immutable by design and this is the owner's own PII, so the options are (a) accept and close — the address is already public in git commit metadata, making the archive copies no additional exposure, or (b) a history rewrite, which breaks the `INDEX.jsonl` append-only witness and every downstream clone. Close this row either way; do not leave it Pending as a standing reminder. | docs | governance | P3 | — | tiny-fix | Pending | — | — |
| 171 | TruffleHog's Lob detector false-positives on ordinary test identifiers, and `--only-verified` does not bound it. The detector's key pattern (`pkg/detectors/lob/lob.go:27` at v3.96.0) is a word-boundary match on `live` or `test`, an underscore, then **exactly 35** characters from `[a-zA-Z0-9_]` — underscores included, which is why an ordinary snake_case function name can satisfy it. **Demonstrated live on PR #402**: the test written to enforce #166's scanner pin had a name of exactly that shape, TruffleHog's Lob verifier returned **VERIFIED**, and `Secret Detection` failed — the PR pinning the secret scanner was blocked by the secret scanner, on the name of the test enforcing the pin. **AC-3 states `--only-verified` exists to bound false positives; it did not bound this one**, so the guarantee that flagged findings are real does not hold for this detector. Scope: a scan of the committed tree finds **35** identifiers of the same shape across 23 files. *(An earlier draft said 36 — that count was taken while the offending new test was still present in the worktree, so it included a transient match that is absent from the merged state. Independent review recounted two ways and got 35.)* They are latent, not currently failing, because the scan is range-scoped — deliberately NOT enumerated here, since quoting even one re-creates the pattern in this row's own diff and would fail CI on the row itself. Reproduce by compiling the detector's regex (read it from the pinned `lob.go`, do not retype it — an earlier hand-written attempt omitted the underscore from the character class and found nothing) and running it over every path from `git ls-files`. **Operationally important fact, established empirically on #402 and not documented anywhere**: the action walks **each commit's diff across the range**, not the net endpoint diff — a follow-up rename does NOT clear a finding, and neither does a net-zero add-then-remove; only removing the introducing commit from the range does (verified: rename push stayed red, squash push went green). Commit **messages** are in scope too — the squash's own message described the incident, re-instantiated the pattern, and had to be reworded before pushing. Options to decide at implement, none taken here because weakening a detector does not belong in a PR whose subject is strengthening this scanner: (a) `--exclude-detectors=lob` in `extra_args` — kills the class outright, costs one detector for a service this repo has no integration with, and the `credential-scan` job remains as a second layer; (b) leave it and absorb the occasional block, documenting the squash remedy in `repo-gotchas`; (c) upstream a bug report on the Lob verifier returning verified for non-credentials. Preference: **(a)**, with the squash remedy from this row documented in `repo-gotchas` alongside it. An earlier draft said "prefer (a)+(b) together", which is not executable — (a) disables the detector while (b) leaves it enabled and absorbs the blocks; they are alternatives, not a pair. What pairs with (a) is the documentation, not (b). | framework | security | P2 | — | quick-win | Shipped | — | #166; Shipped 2026-08-23 (branch fix/trufflehog-lob-false-positive-171): option (a) taken — `--exclude-detectors=lob` + repo-gotchas #15. Forced by #88, whose committed 897-id durations file failed the job outright on PR #419. Scope guarded by a new mutation-verified test (`test_detector_exclusions_stay_scoped_to_lob`) so a future "just one more" exclusion cannot pass review unseen. |
| 172 | `.agentcortex/context/.guard_receipt.json` is listed in `.gitignore:7` and is **also tracked** — the last member of a contradiction class whose other two members were cleared on 2026-08-13. It cannot simply be untracked: `validate.sh:2140-2145` and `validate.ps1:1996-2002` PASS on `-f` of that exact path and WARN otherwise, so untracking alone trades a real contradiction for a permanent cosmetic WARN on every clean checkout — including every external reviewer's first run, against a `warn=3` baseline this repo reads as a health signal. **This is ADR-002 Phase 3, not a cleanup.** `docs/adr/ADR-002-guarded-governance-writes.md:230` already specifies it: drop `legacy_receipt_mirror`, deprecate the legacy DEFAULT_RECEIPT path, and have validators key off the `.guard_receipts/` directory instead. Blocked on a prerequisite that has been **superseded, not satisfied**: Phase 3 is gated on "one release after Phase 2 stabilizes", and Phase 2's first item — guarding the `INDEX.jsonl` append — is now actively forbidden by `ship.md:208` ("Do NOT call `guard_context_write.py append` for `INDEX.jsonl`"), because `append_chain_entry.py` owns chain awareness. So the gate condition can never be met as written and the ADR needs an amendment saying so before Phase 3 runs. Evidence that the mirror is already redundant: `.guard_receipt.json` and `.guard_receipts/337ffd90d88a8b4f.json` resolved to the **same blob** `c50b26a6` at `51cf78f` — the legacy mirror is a byte-identical duplicate of a per-target receipt. Work when picked up: amend ADR-002 with the corrected Phase-2/3 gate, flip `legacy_receipt_mirror` off in `.agent/config.yaml`, change both validators to PASS when the legacy file exists **or** `.guard_receipts/` is non-empty (sh/ps1 parity is mandatory — see the `[paired-check-parity]` Global Lesson), then `git rm --cached` the legacy file. Do NOT do the `git rm --cached` first. | framework | governance | P3 | — | quick-win | Pending | — | ADR-002 |
| 173 | Downstream can WRITE the hash-chained audit log but nothing VERIFIES it, and the guard that should have caught this is blind to the whole class. `ship.md:202` (deployed) instructs every downstream ship to append to `INDEX.jsonl` via `append_chain_entry.py` (deployed), and `check_audit_chain.py` was **not** in `deploy.sh`'s runtime-tools whitelist — it is now (`git log -S check_audit_chain -- .agentcortex/bin/deploy.sh` → no commits). Proven by simulation, not inferred: a fresh deploy + one real chain append + the deployed `validate.sh` → `[SKIP] audit chain integrity (INDEX.jsonl) -- tool not present`. ADR-003's tamper-evidence is therefore write-only downstream. **RESOLVED 2026-08-16** — the decision this row needed was: deploy the checker, or record it as deliberately source-only. Both have precedent — `check_skill_provenance.py` (#379: "a source-repo-only validator that deploy.sh deliberately does not ship") and `check_worklog_references.py` (#161: "source/CI-only per the #137 precedent") are intentional; but ADR-003:140 names "fresh downstream" as a case its design handles and nowhere scopes the chain to the source repo, and the check is wired at **FAIL** severity (`validate.sh:398`), not as a CI-only SKIP. Note the SKIP wording does **not** discriminate: deliberate source-only tools also print "tool not present", so an earlier reading of that string as evidence of oversight was wrong. **Wider than one tool**: the deployed validators reference 19 tools of which **7** are absent (`check_audit_chain`, `check_routing_actions`, `check_skill_provenance`, `check_worklog_references`, `generate_compact_index`, `update_lifecycle_baseline`, `validate_trigger_metadata`) with no allowlist separating deliberate from accidental — so the next omission is equally invisible. **Why the existing guard missed it**: `tests/ci/test_deploy_tiering.py::test_deployed_governance_referenced_tools_are_deployed` scans only AGENTS/CLAUDE/GEMINI plus `.agent/workflows/` and `.agent/rules/` markdown for the literal path `.agentcortex/tools/<name>.py`; `ship.md` names this tool in prose as a bare backticked filename, and the deployed `validate.sh`/`validate.ps1` — which reference tools by full path — are not in the scan set at all. Work when picked up: decide per-tool, add an explicit `SOURCE_ONLY_TOOLS` allowlist with a one-line reason each, then extend that test to scan the deployed validators against it (red first: the extension must fail on today's tree before the allowlist is added). Whatever is decided for the checker, `ship.md`'s prose was corrected in the same wave that found this — it no longer claims downstream detection. | framework | governance | P2 | — | quick-win | Shipped | — | ADR-003 **Shipped 2026-08-16 (branch `fix/validator-downstream-truth-claims`)**: the checker is deployed (both `deploy.sh` sites per frozen-spec AC-S5 + regenerated golden), so the chain is verified downstream rather than write-only. The prescribed `SOURCE_ONLY_TOOLS` allowlist was **rejected** — `IS_SOURCE_REPO`/`$isSourceRepo` already exist and the allowlist's payload is a comment; instead a deliberately-unshipped tool states its reason at the call site (`run_python_check_source_only` / `-AbsentReason`), and an absence with NO stated reason is what the summary line counts and what CI fails on. Do NOT re-propose the allowlist. Of the seven absences: two are confirmed deliberate (`check_skill_provenance`, `check_worklog_references`), three already had honest guards (`validate_trigger_metadata`, `generate_compact_index`, `check_routing_actions`), `update_lifecycle_baseline` was the lifecycle-ladder fix, and `check_audit_chain` now ships. Honest residual ceiling: a whitelisted tool whose call site guards on its OWN presence and emits its own reassuring SKIP (verified: `generate_safety_nucleus.py`) still leaves CI green — the deployed FILE SET is pinned by `deploy_manifest_golden.txt` instead. |
| 174 | Validator twin drift: `validate.sh` and `validate.ps1` give different answers on the same tree. (a) `validate.sh:2531` selects backlog rows with a grep alternation matching Pending OR In-Progress, while `validate.ps1:2360` matches Pending only — different row sets, different reported counts; the sh pattern's second alternative is unanchored (no leading pipe-space), so In-Progress also matches prose cells, and sh is internally inconsistent because its L-1/L-3/L-3b siblings select Pending only. (b) archive size: sh `du -sk` (disk-allocated) vs ps1 summing file `.Length` (logical) — measured 2274KB vs 1786KB over 179 files. ps1 is semantically right (the threshold is an ingestion-cost proxy) but `du --apparent-size` is GNU-only, so use a portable sum. (c) `validate.sh:2794` gates its PASS on a bare `*.md` glob counting the `_*` meta files the scanning loop just skipped (NOT `.gitkeep.md` — bash `*.md` never matches a dotfile without dotglob; an earlier draft of this row said otherwise and was wrong), so a fresh downstream whose `docs/specs/` holds only placeholders gets a vacuous PASS asserting validity over zero governed specs. Reported by a downstream adopter on v1.8.21; all three verified against code. A full label-set parity test was measured and **rejected** (131 vs 153 literals; most divergences are `${var}` interpolation artifacts needing a normalizer whose failures are undiagnosable). Dominating substitute: assert the two validators produce matching pass/warn/fail/skip tallies on one fixture tree — it would have caught all three — but first confirm `validate.ps1` runs under `pwsh` on `ubuntu-latest`, since `Pytest (Windows)` is non-required and a Windows-only placement can go red and still merge. | review-finding | tooling | P2 | — | quick-win | Shipped | — | — **Shipped 2026-08-17 (PR #414, v1.8.22)**: all four divergences closed — anchored whole-cell active-row selection on both sides (ASCII space/tab class, case-sensitive; ps1 got its own `$activeRows` so `$pendingRows` stays Pending-only for L-1/L-3/L-3b), both validators now sum archive bytes and both floor, the frontmatter PASS is gated on the filtered count with a SKIP rather than silence when there are none, and the Evidence PASS message matches. Also fixed and verified **pre-existing**: the label selector ran under `set -euo pipefail` unguarded, so an all-Shipped backlog aborted the whole validator mid-run. The prescribed fixture-tree tally-parity test is **still not built** and stays open — the guards added here are source-text, in the style the repo already sanctions, because `validate.ps1` mis-resolves `$root` under Linux `pwsh` and cannot be run there. |
| 175 | `validate.ps1` never sets `[Console]::OutputEncoding`, so `§` and `—` render as mojibake in a non-UTF8 Windows console; `validate.sh` is unaffected. Reported by a downstream adopter. Narrower than it first looks: the file carries a UTF-8 BOM so literals parse correctly even on PS5.1, only 25 of 166 non-ASCII characters are on printed lines, and — **corrected 2026-08-23 by measurement** — it DOES reproduce on `pwsh` 7 as well as `powershell` 5.1 (identical big5 bytes on a cp950 box); the original "does not reproduce on pwsh 7" claim was wrong. The fix must NOT set the property directly — it is process-global and the script runs in the caller's live session, so it would mutate console state after exit; use `try/finally` save-restore. Touches a file pinned by the encoding canary, so run the full CI-equivalent suite. | review-finding | tooling | P3 | — | quick-win | Shipped | — | Shipped 2026-08-23 (branch fix/validator-twin-parity-176-175): try/finally save-restore; MFR + fix verified on both shells; EOL/BOM preserved. |
| 176 | `run_python_check`'s `missing_python_level` (`validate.sh`, 15 call sites) and `Invoke-PythonCheck`'s `$MissingPythonLevel` (`validate.ps1`, 15 sites, declared `Mandatory`) are assigned and never read: with Python absent every check degrades to WARN regardless of the severity the call site requested. Do NOT implement the parameter's semantics — measured, that flips five FAIL-wired checks referencing deployed tools to FAIL on every Python-less adopter on install day, which the framework's no-python doctrine explicitly permits. Live options: delete the parameter (28 call-site edits; `shift 3` to `shift 2` in bash is the riskiest edit in this family — a missed site silently turns `$script` into the string FAIL, the check becomes a SKIP, and exit stays 0 with CI green, so the receipt must be byte-identical validator output), or keep it and document the always-WARN behaviour as deliberate. **2026-08-23 decision (3-panel + primary re-verification): DELETE.** Not a quick-win — real scope is 30 call sites + `test_validator_absent_tool_signal.py`'s arity regex + 3 tool docstrings naming "WARN-tier wiring" + a line-number anchor in `check_worklog_references.py` + `decision-capture-hardening.md` AC-6; crosses the `>2 modules` hard-block, so re-bootstrap as its own unit. Root cause found: the parameter is the vestige of a deliberate removal in `e9355c7` (#51), which replaced `record_result "$missing_python_level"` with a hardcoded WARN. Required guards: (a) assert the required `validate` job's final line is exactly `Agentic OS integrity check passed`; (b) retune the source-only regex to the new arity — do NOT loosen it. Risk is bash-only (PowerShell named params fail loudly). The annotation is not merely dead but wrong where it matters: three WARN-declared tools have no nonzero-return path at all, and the one that does (`validate_downstream_capabilities.py`) is mapped to FAIL by the wrapper. | review-finding | tooling | P3 | — | quick-win | Pending | — | ADR-006 |
| 177 | `deploy.sh:739` (the `_runtime_tools` string) and `deploy.sh:928` (the `runtime_tools` array) are byte-identical 23-entry duplicates ~190 lines apart — the reason shipping one tool takes three edits. Hoisting the array and referencing it in both places is a pure deletion. **Blocked on a spec amendment, not a refactor.** **2026-08-23 update (#178): the frozen-gate half of this blocker is GONE** — `docs/specs/downstream-adaptability-optimization.md` is now `status: shipped` (reconciled against the SSoT Spec Index, which had recorded `[Shipped 2026-06-14, PR #238]` all along), so no unfreeze is required. What still blocks is the **AC-S5 wording itself**: it mandates adding a tool to both `deploy.sh` sites, explicitly guarding against a one-spot half-fix. Collapsing two sites into one satisfies that intent but contradicts the AC as written, so it needs a spec-freshness update (`ship.md §6`) recording the collapsed shape before the refactor lands. Partial mitigation already shipped: `tests/ci/test_validator_absent_tool_signal.py` asserts the two sites list the same tools, so silent drift is now caught even while the duplication stands. | review-finding | deploy | P3 | — | quick-win | Pending | — | ADR-005 |
| 178 | Conflicting directive between two governance surfaces, surfaced during a hotfix and deliberately not patched there. `AGENTS.md` §vNext State Model §Write Isolation scopes backlog writes to `_product-backlog.md` updates during spec-intake/ship, while `bootstrap.md` §1.5 **mandates** a `Pending → In Progress` advance at bootstrap and calls it the only valid such transition. Precedence (AGENTS.md > workflows) would forbid the step the workflow requires. Same axis as the shipped `docs/specs/conflicting-directive-scan.md`. Resolve by widening the AGENTS.md enumeration or moving the advance to a later phase — one line either way, but it is a governance-surface edit and needs its own unit. Smaller item found in the same pass: `docs/specs/downstream-adaptability-optimization.md` frontmatter still reads `status: frozen` while the SSoT Spec Index records it `[Shipped 2026-06-14, PR #238]`, and §4.2 says `/ship` sets `shipped`. | review-finding | governance | P3 | — | quick-win | Shipped | — | Shipped 2026-08-23 (branch fix/agents-md-backlog-write-scope-178): AGENTS.md enumeration widened to name the bootstrap status advance; the same line's two duplicate no-Python fallback clauses merged as the §13 trim; directive ratchet held at 37/37. Spec status reconciled frozen→shipped. |
| 179 | `/handoff §6` contradicts itself once a log actually hits the cap: step 2 says "Move older details to `archive/work/`", step 4 lists `## Evidence` among sections that "MUST remain in the active Work Log and MUST NOT be summarized, folded, or rewritten". A log whose Evidence is the bulk of its bytes therefore has no compliant way to get under `max_kb`. Hit live on 2026-08-23 (fix/validator-twin-parity-176-175, quick-win): the log reached 22.7KB against a 12KB cap and `validate.*` FAILed on `work log compaction warnings detected`. Worked around by moving *completed-phase* evidence verbatim with an in-place pointer — a reading step 4 permits only if "moved verbatim" is distinct from "summarized/folded/rewritten", which the text does not say. Same axis as #178 (surface-vs-workflow conflict) and adjacent to #135 (no quick-win-lite template): a quick-win whose /plan runs an adjudication cannot fit the cap. Resolve by scoping step 4 to the CURRENT phase's evidence, or by naming verbatim-move as an allowed operation. | review-finding | governance | P3 | — | quick-win | Pending | — | — |
| 180 | `pytest.ini §norecursedirs` omits `.claude`, so a leftover agent worktree under `.claude/worktrees/<id>/` makes a bare local `pytest` double-collect the whole test tree: measured 2026-08-23 as `877 collected, 25 errors` ("import file mismatch") against `897 collected, 0 errors` for the CI-equivalent explicit paths (`tests/ci/ tests/guard/ .agentcortex/tests/`). CI is unaffected because `validate.yml` always passes explicit paths, so this is a local-DX trap only — but it reads as a broken suite and invites the wrong diagnosis. One-line fix: add `.claude` to the existing `norecursedirs` list (which already prunes `.acx-local`, `.git`, `scratch`). | review-finding | tooling | P3 | — | tiny-fix | Pending | — | — |
| 181 | No macOS CI runner exists — measured 2026-08-23: `.github/workflows/*.yml` declares **15 `ubuntu-latest` + 2 `windows-latest` + 0 `macos-latest`**, so the platform a large share of adopters actually develop on has zero coverage. Not filed on a suspected break: a BSD-vs-GNU scan of the two shipped shell scripts came back **clean** (`find -printf` / `xargs -r` / `sed -r` / `sed -i` / `readlink -f` / `date -d` / `stat -c` / `du -sk` / `grep -P` all zero unguarded uses) and `sha256sum` already carries a deliberate `shasum -a 256` → `openssl dgst` fallback with a GNU-vs-BSD escaping note — i.e. someone accommodated macOS on purpose. The gap is that **nothing would catch a regression if one appeared**. Low-risk to add: the CI deps are pure-Python (pytest / PyYAML / pytest-split, no compile step on ARM) and the 29 `requires_windows` + 30 `requires_powershell` gates are `skipif`-on-tool-presence, so they degrade rather than error — the job is a near-clone of the ubuntu `CI Structural Tests` step with `runs-on: macos-latest`. **Constraints agreed with the owner**: land it **non-required** (branch protection untouched), treat the first real run as the verification since the change cannot be tested locally, and if it goes red bring the failure back for a decision — do NOT make it green by weakening a test or adding a skip. | dx | ci | P2 | — | quick-win | Pending | — | — |
| 182 | No test pins version consistency across the seven release surfaces. A release bumps `deploy.sh` `ACX_VERSION`, `CITATION.cff` `version:` **and** `date-released:`, both `TESTING_PROTOCOL*.md` titles, `antigravity-v5-runtime.md`, and both `AGENT_MODEL_GUIDE*.md` titles — all by hand, verified by a grep the releaser has to remember to run. Miss one and nothing fails: no validator check, no pytest, no CI job compares them. Same failure class as `repo-gotchas` #12 (a release is not finished when the PR merges), which exists because the tag step has been forgotten **twice**. Filed on a verified absence measured during the v1.8.23 cut: `grep -rln` over the seven paths finds the version string in each, and `grep -rn` over `tests/` finds no assertion that they agree. Cheap fix: one test that reads `ACX_VERSION` as the source of truth and asserts the other six carry the same `MAJOR.MINOR.PATCH`, plus that `CITATION.cff` `date-released` is not older than the newest CHANGELOG heading. That makes the release checklist machine-enforced instead of honour-system — the [enforcement] Global Lesson applied to the release path. | dx | ci | P2 | — | quick-win | Pending | — | — |
| 183 | Workflow job-graph integrity is unguarded — a dangling `needs:` makes GitHub reject the **whole** workflow at parse time, not skip one job, so every push-triggered check silently stops running. Reported by a downstream fork (HabitFlow, 2026-08-24) who hit it with a fully green local tree: `flutter analyze` 0, 9994 tests passing, `validate.sh` 0 warnings — because the branch was unpushed and nothing in the repo parses `.github/workflows/`. Here the exposure is narrower but real: `tests/ci/test_security_workflow.py:46,557` `yaml.safe_load`s both workflows (so a syntax error is caught) and `:551` pins core job existence, but a dangling `needs:` is **valid YAML** and no test resolves the graph. Measured 2026-08-24 on this tree: `security.yml` 5 jobs, `validate.yml` 12 jobs, **0 dangling** — filed on a verified absence of a guard, not a live break. Fix: ~10 lines in `test_ci_hardening.py` — load each workflow, collect job names, assert every `needs:` target is in the set. New-check rules do not apply (pytest-only, nothing deployed). | framework | ci | P3 | — | quick-win | Pending | — | — |
| 184 | No PowerShell version floor, and CI never exercises Windows PowerShell 5.1 — the interpreter the shipped pre-commit hook actually invokes. `.githooks/pre-commit.guard-ssot.sample:65-67` calls `powershell -ExecutionPolicy Bypass -File validate.ps1` on a Windows Git shell, i.e. **5.1**, while `validate.yml:185` runs only `shell: pwsh` (7.x). No `.ps1` in the tree declares `#requires -Version`. Measured 2026-08-24: zero PS7-only constructs present (`EnumerateRunes`, `[System.Text.Rune]`, `-AsHashtable`, `ForEach-Object -Parallel`, `??`, `?.`, `-AsByteStream`, `Join-String` all absent), and `validate.ps1` both parses and runs clean under 5.1.26100.9168 — so this is a missing guard, not a live break. The same shape bit a downstream fork, whose `check_text_integrity.ps1` used `EnumerateRunes()` and crashed on 5.1 (their report, 2026-08-24, item S2.2). Fix: add `#requires -Version 5.1` to the shipped `.ps1` files **and** a CI step that parses every `.ps1` under `powershell.exe` — parse-only is cheap and catches the API class; a full 5.1 run of validate.ps1 is the stronger version and needs a cost decision. | framework | tooling | P3 | — | quick-win | Pending | — | — |
| 185 | Backtick-path references in governance docs are never existence-checked — the CI `markdown-links` job (`validate.yml:144-171`) only resolves markdown inline links (bracketed text immediately followed by a parenthesised path), while the dominant way this repo cites a mechanism is an inline code path (`` `.agentcortex/tools/foo.py` ``). A rule can therefore name an enforcement mechanism that has been deleted and nothing notices; that exact failure was reported by a downstream fork (2026-08-24, item U3) whose `gate:`-tagged lesson pointed at a deleted enforcer. Scanned 2026-08-24 over `.agent/rules/`, `.agent/workflows/` and `.agentcortex/docs/` (excluding `_zh-TW` twins): **0 genuinely dangling** references — every raw miss was a bare basename, a historical "this file was never created" mention, or a conditional fallback. Deliberately **no total is quoted**: the count depends entirely on how "path-shaped" is defined, and three reasonable predicates over that same scope disagree by more than 2x. That ambiguity IS the work — a scan whose denominator nobody can reproduce would be the same defect this row is about. Filed on a verified absence of a guard, not a live break. Fix must handle those three false-positive classes or it will be muted on arrival; that design is the work, not the scan. Related: #161 (Work-Log `## External References`, shipped) solved the same shape for one field. | framework | governance | P3 | — | quick-win | Pending | — | — |
| 186 | `token-governance.md §8`'s compaction target collides with the archived-Work-Log contract — §8 step 2 tells an agent at the size cap to move older entries to `.agentcortex/context/archive/work/<key>-<YYYYMMDD>.md`, but `validate.sh:2204` / `validate.ps1` treat **every** `.md` under `.agentcortex/context/archive/` as an archived Work Log requiring a non-empty `## Phase Summary`, excluding only `ship-history-*` and `global-lessons-archive*`. So following §8 literally produces a WARN every time, and `tests/ci/test_validator_false_positives.py::test_171_ship_history_no_phase_summary_warn` goes red because it asserts that string is absent from this tree. Hit live 2026-08-24 while compacting `fix-ignore-assertion-binding.md`; worked around by giving the fragment a real `## Phase Summary`, which is defensible for that file but does not generalise — a fragment is not a Work Log. Resolve by either adding a third exclusion pattern for compaction fragments (naming convention needed) or by having §8 write them somewhere the archived-log scan does not claim. Same axis as #179 (`/handoff §6` contradicting itself at the cap). | framework | governance | P2 | — | quick-win | Pending | — | #179 |

> Rows whose GH issue is CLOSED-premature (#7, #13, #18, #21, #33) are kept deliberately as
> future directions — reopen the issue when a concrete signal appears (2026-06-02 curation).
> Rows #76–82 (added 2026-06-19) came from the external skill/workflow-practices research (Node-15 synthesis). #76 was reduced to a lightweight `research.md` note-taking convention and the heavier capsule design (ADR-009 + draft spec) retired — external prior art (Anthropic context-engineering + multi-agent research system) treats research-state persistence as lightweight "structured note-taking", not infrastructure, and agentic-os already externalizes to Work Logs / private notes. **Reopen-triggers (record-only, no issue opened):** G2 byte/installer-verification → when an external skill-installation capability is separately approved; E context-free author testing → when #79 is built; F isolation-aware worktree portability → when a non-git-worktree platform target appears (must not duplicate existing worktree rules).
> 2026-06-19 (Option A): #80+#81 merged on branch `feat/skill-provenance` as ONE quick-win ("SKILL.md compatibility + provenance floor"); #81 right-sized feature→quick-win (static manifest + completeness check); its heavy half (digests/byte-verify/exception-schema) stays under the G2 reopen-trigger above. #82 rides along. #83 (skill/workflow content-optimization research vs reference repos) added, queued AFTER this branch ships.
> Rows #84–86 (added 2026-06-20) are the un-done follow-ups from the README revisual (**PR #262**, merged `318d8f2` — a 3-visual hook→proof→system overhaul, EN + 繁中, with `concept-hero`/`workflow-demo`/`pipeline-demo` in both languages). Full context + what shipped is in the `Ship-docs-readme-revisual-2026-06-20` entry of `current_state.md`. **#84 (CI-onboarding) is the highest-value** — a final-review persona called it the real conversion leak (the README leans on "CI is the unbypassable floor" but never shows an adopter how to wire it). #85/#86 are parity + polish.
> Completed entries (#2, 4–6, 8–10, 12, 14–16, 19–20, 22–32, 34–44, 51, 66–68, 71, 73–75) are in [`_product-backlog-archive.md`](./_product-backlog-archive.md).

> 2026-06-20 batch close-out: #84 shipped (PR #263). A cross-platform entry-point diagnosis + the README follow-ups spawned: **Copilot deploy parity** (PR #264 — `.github/copilot-instructions.md` now ships downstream), **ship.md Ship-History ordering fix** (PR #265), **#86 README de-dup prose** (PR #266), and the **using-git-worktrees Step-0 DO-NOW** from #83 (PR #267). **#86 → Shipped** (prose de-dup done; the GIF-unification half is a reopen-trigger — needs binary-asset re-render + visual verification we can't reliably do). **#83 → Cancelled** (skill content verified SOUND vs superpowers v6 by independent experts; broad "needs content optimization" premise disconfirmed; only the worktrees DO-NOW was actionable → PR #267; reopen only if a concise skill is shown to cause a real failure). **#85 stays Pending, blocked on owner Cursor access** — the Cursor compatibility *claim* is VERIFIED (Cursor natively reads AGENTS.md; PR #264 diagnosis), so the residual deliverable is a real in-Cursor screenshot/GIF (owner-only) + an optional Cursor platform guide + the `CLAUDE_PLATFORM_GUIDE.md` `_zh-TW` twin.

> 2026-06-20 (zh-twin ship, branch `docs/claude-guide-zh-tw`): **#85 → Cancelled.** The `CLAUDE_PLATFORM_GUIDE_zh-TW.md` twin (the AI-doable residual) shipped, now deploys downstream (deploy.sh glob + managed[]), and is linked from the 繁中 README. Verified the **Cursor (AGENTS.md native read; README:159/195) + Copilot (`.github/copilot-instructions.md`, deployed #264) entry points already ship** — so an earlier "cancel Cursor support" framing was WRONG; what's cancelled is only the **owner-only in-Cursor screenshot/GIF** (owner has no Cursor — marketing polish, not a functional gap). **Reopen-trigger:** owner gains Cursor access + wants the visual. Cursor platform guide **declined** (thin; README/reference already document the Cursor entry). Spawned **#87** (INSTALL zh-TW twin — the #84 EN-only `docs/INSTALL.md` residual).

> 2026-06-28: #97 surfaced by a governance-only comparison sweep against a reference implementation; verified absent here (routing_actions has field-validation + a ship-time current-domain check, but no age escalation — a pending action for a non-current domain can persist unwatched). Backlog-only per issue-exposure policy (small hardening of an existing gate, not feature-like; no observed incident → P3).

> 2026-06-28 (downstream-adaptability research): #98–101 from a read-only 3-axis adaptability diagnosis (skill coexistence · multi-person scaling · complex adoption scenarios). #98/#99 backlog-only — no verified consumer today (solo repo / no floor-only request). #100/#101 are report-trigger **issues** (#291/#292), feature-like, opened to gather real adopter signal per issue-exposure policy *before* building. Most adaptability axes were already-handled or out-of-scope-by-design (ADR-004/005/007/008); the three small do-now honesty/hardening fixes (skill-ecosystem status note, INSTALL monorepo note, bootstrap owner-default) shipped directly, not via backlog.

> 2026-07-06 (full-project audit routing): #118–#125 route the 2026-07-06 three-agent audit + primary spot-verification (overall grade C; root-cause thesis: discovered-not-converting because a default install has zero active machine enforcement and day-1 is interruption-dense — **fix conversion, not features**). Dispatch this session: #95+#116+#118 → branch `fix/audit-quickwins-batch`; #117+#91 → branch `fix/lesson-chain-archival` (both touch append_lesson.py, batched to avoid conflict). Suggested pick order for follow-up sessions: #120 → #121 → #119 → #113 → #122 → #124 → #125 → #123. **Watch-items (record-only):** (a) token ceiling headroom ≈0 — counted-doc additions stay DELETION-FUNDED; refuse scope past ~345k without a paired deletion; (b) two 2026-07-06 subagent claims are UNVERIFIED — re-check before acting: ".agents/ vs .agent/ skills trees are identical full copies" (AGENTS.md documents stub-vs-canonical by design) and "skill-research-integration.md is a shipped spec" (absent from the SSoT Spec Index); (c) sentinel enforcement nuance for future audits: the work-log ⚡ ACX marker HAS a WARN-tier validator check (validate.sh ~L1509/L1702) — only the per-response sentinel is self-attested; (d) #120/#121 are the adoption bet — new features stay frozen until conversion signal (stars/issues/return visits) moves.

> 2026-07-09 (post-v1.8.9 reconcile): **#119 → Shipped** via v1.8.9 (PR #324) as **R1 — a framing clarification** (§4.4 already accepted a file-path artifact; the gate stays HARD), NOT the capability-seam escape this row originally proposed. A 5-agent roundtable + 第十人 + 事前驗屍 UNANIMOUSLY rejected the `design_tool` capability-seam path (verified: reincarnates ADR-001 D2's rejected flag; `validate_downstream_capabilities.py`'s allowlist makes gate-relaxation unrepresentable → an unknown key rejects the whole file). The deeper finding — the design gate is honor-system theatre — folds into **#122** (enforce-vs-delete), NOT a new row. v1.8.9 also packaged already-merged #319/#321/#322 (adapter same-turn #118, Global-Lessons chain-archival #117, ask-local sync). **Full roundtable record (cross-platform)**: `docs/reviews/2026-07-08-design-gate-roundtable.md`.

> 2026-07-10 (complaint-driven audit routing): #126-#135 route the 2026-07-10 four-angle audit (day-1 friction / ceremony-cost / enforcement-gap / active-harm) + 3 second-wave finders (test-teeth, doc-consistency, month-3 lifecycle) + 2-expert panel adjudication. Dispatched this session: #126 (branch fix/claude-stub-guardrails-slim), #127 (chore/ssot-caps-enforcement), doc-consistency batch (fix/doc-consistency-batch: worklog-template Test Gate Results gap, zh-TW gate-critical mirror drift ×2, state_machine escalation-list subset, AGENTS.md quick-win transition wording, routing.md skill/command registry gaps). Closed-with-reason (no rows): shared-contracts per-phase re-read (deliberate Read-Once exemption, AGENTS.md:27); deploy add-only accumulation (ADR-005 never-delete by design; reopen on adopter gc request); backlog row growth (manual archival chore precedent); docs-PR heavy-test skip (dev-flow-hardening AC-13 deliberate; #112 shipped the docs-pins job); the 12 non-guardrails Required-reads stub blocks (cosmetic). Conversion thesis unchanged: #120 → #121 remain the top picks; none of #126-#135 outrank them.

> 2026-07-11 (codex audit-wave remediation): codex two-pass + adversarial governance audits (PRs #337/#338 — 3 review snapshots, 10 findings, deduped against #105/#108/#109/#113/#114/#116/#120/#122/#134) → **all 10 primary-verified real, 0 false alarms** (F10 broader than reported: punctuation chars break current-branch detection on BOTH platforms, not just case on bash) → remediated same-day in 3 delegated PRs, all merged CI-green: **#339** (executor safety — §8.2 canon: pre-flight baseline capture, abnormal-exit stop-and-reconcile, baseline-dirty never whole-file-reverted, Requested/Actual Executor provenance; enforced by 7 docs_pin tests), **#340** (validator fail-open — directive-anchored command-sync, manifest-agnostic adapter validation, structural routing_actions parse via check_routing_actions.py behind the ADR-006 seam; 17 new tests), **#341** (receipt integrity — bootstrap 5-step canonical worklog-key normalization in both validators + Timestamp/Classification/SHA value validation on the existing WARN-tier checks; 19 new fixtures). All 10 routing_actions rows flipped merged; L2 document-governance entry appended. New rows: #136 (ps1 FAIL double-count parity, found by WP3 beyond the audit), #137 (routing-tool downstream promotion, deliberate WP1 non-goal). Closed-with-reason (no rows): Timestamp monotonic ordering (order-of-appearance authoritative; clock-skew false-positives); reclassification epoch parsing (WARN-tier header binding suffices); GPT-1.0 residual wording + vestigial `.agents/workflows/` duplicates (both fixed/deleted in the wrap-up PR — zero references, verified both directions). Conversion thesis unchanged: #120 → #121 remain the top picks.

> 2026-07-16 (decision-capture audit routing): #138–#139 route the 2026-07-16 govern-audit (user-hypothesis verification: "product decisions land SSoT-only without ADR/spec → drift"; 3 scan + 2 simulation agents + primary verification — confirmed **real-but-bounded**: every ADR/spec-backed decision found durable; the leak is quick-win/hotfix decisions + rejected designs + cap-10 rotation). Closed-with-reason (no rows): bootstrap L1-only/L2-write-only reads (token economy by design), Ship History cap-10 rotation itself (#127 by design — the defect is upstream decision homelessness), Symphony-model 2026-05-06 loss (pre-dev-flow-hardening; reopen trigger: grep archive/feat-epic-spec-hierarchy-governance-20260506.md before any backlog-schema redesign). Full record + routing_actions: `docs/reviews/2026-07-16-govern-audit-decision-capture.md`. Conversion thesis unchanged: #120 → #121 remain the top picks; #139 carries a rotation deadline (~4 ships).

> 2026-07-16 (decision-capture wave close-out): **#139 → Shipped** (PR #348, instance-batch) and **#138 → Shipped** (feat/decision-capture-hardening: ship.md 2b + SIM-W tripwire, decide.md §5 truth-rewrite, template §Decisions, check_decision_disposition.py Signal A + A2 with non-clearing remediation, bootstrap:143 none-guard; both audit routing_actions → merged). Verified by 第十人+事前驗屍 (12 dispositions) + a 4-sim behavioral wave (write-path, read-path drift replay — original scenario now hard-stopped rotation-proof, violation loop, downstream deploy ×4). **Watch-item (record-only, no row)**: config.yaml is deploy-tier core (force-updated on upgrade) so fork-tunable keys (production_paths, decision_disposition_since) do not survive framework upgrades — pre-existing tension, no observed incident; reopen if a fork reports lost config customization or an upgrade WARN-flood.

> 2026-07-22 (external-research wave close): the codex external-repo research handoff (tracked verdict summary: `docs/reviews/2026-07-22-external-research-verdict.md`; local working note `.agentcortex/context/private/research-external-repos.md` is gitignored per the #76 convention; roundtable+第十人+premortem-verdicted) executed as a 2-PR delegated wave, every handoff claim primary-verified against ground truth first — **#107 → Shipped** (PR #358, sonnet delegate: coverage matcher exact-or-explicit-prefix + §4.5 anchor normalization), **#113 → Shipped** (PR #359, opus delegate: reduced-assurance top-line labeling in both validators + 6-test malformed-log corpus; ps1-stronger asymmetry documented, equalization stays #136/#140), **#89 → Shipped** (records-only reconcile — enforcement had already shipped 2026-07-17 via PR #345). New row **#143** routes the 28/45 zero-coverage eval-drift finding disclosed during #107 (primary-replicated on main). Recorded handoff deviations (primary judgment): #142 reachability / three-client skill-discovery / plugin field-mapping fixture experiments NOT run — no current consumer; survivors #33-mapping-spike/#122-deletion-funded keep their verdict constraints; #77 schema stays parked behind #124 revalidation. Conversion thesis unchanged: **#121 is the top pick** (#120 shipped 2026-07-21).

> 2026-07-27 (**#78 stays parked — investigated, refuted, not built**): a daily-triage pass on issue [#253](https://github.com/KbWen/agentic-os/issues/253) proposed slicing off a "Piece 1" (runtime read-only ceiling on the delegated reviewer + a non-implicit-pass `cannot-verify` verdict) on the theory that it was unblocked by #77/#252. A 4-seat roundtable + one external signal (`code.claude.com/docs/en/sub-agents`, the authoritative frontmatter contract, fetched per the `[audit-method][HIGH]` lesson) **refuted the slice**, and every finding below was primary-verified against the tree before acceptance. (a) **The ceiling is unbuildable at the granularity offered**: `/review` performs 11 writes, one of them to a git-tracked file (`review.md:259-263`, backlog finding registration); `tools:`/`disallowedTools:` are tool-name granularity with no path specifiers, and `settings.json` `permissions.deny` is session-global (it would also gag the primary and `acx-implementer`). The minimum viable form, `disallowedTools: Write, Edit, NotebookEdit`, leaves `Bash` — so it blocks the accidental hazard only and must never be described as a boundary. (b) **The hazard is unproven**: no archived Work Log shows `acx-reviewer` editing reviewed code; `arch-frozen-spec-lifecycle-20260623.md:31` shows the primary holding the lock and writing receipts. (c) **The mechanism would be duplicative**: `subagent_policy: read-only` (ADR-007:79, bound at `bootstrap.md:120`) already declares "the primary stays the sole Work Log writer, gate owner, and sentinel emitter" — declaration-only; the missing half is enforcement of that key, not a second key. (d) **The `[NEEDS_HUMAN]` carve-out is a LIVE check, not stray prose** — `validate.sh:1710-1714` and `validate.ps1:1631` deliberately exempt tagged rows from the MEDIUM-1 WARN; changing it is changing enforcement (the PR #367 class, where two proposed fixes would have silently disabled live checks). (e) **The naive fix would have re-created its own bug** — see new row #148. (f) **Token ceiling**: aggregate measured at **354,785** against the 355,000 cap (`test_lifecycle_token_consumption.py:499`), and `review.md` carries a **9×** load multiplier, leaving ~95 chars of net addition — any new verdict section is deletion-funded. (g) A bare `Task quality:` receipt axis parses but is **unchecked** by either validator — the `[enforcement][HIGH]` "honor-system theatre" pattern. Reopen criterion at `docs/reviews/2026-07-22-external-research-verdict.md:43` (A/B defect-yield justifying token/wall-time cost) is **unchanged and unmet**. Salvage from the investigation: new rows **#147** and **#148**, both pre-existing defects unrelated to #78.

> 2026-08-09 (wave close + pick order): the 2026-08-08 task-simulation govern-audit wave closed **7/7** (#158–#164 all Shipped; PRs #387–#396, v1.8.19 released; full record: SSoT Ship History + `docs/reviews/2026-08-08-govern-audit-task-simulation.md`). **Pick order for the next session**: **#121 stays the top pick** (README enforcement-matrix — the conversion thesis; README is pinned in two layers, grep `tests/ci` + both validators before restructuring). Fast lane, batchable as one branch per the `fix/audit-quickwins-batch` precedent: **#155** (handoff.md §6 one-sentence relative-link warning mirroring ship.md:196) + **#96** (link `docs/ai-contributors.md` from CONTRIBUTING, dodging README pinning); optional add-ons **#90** (BOM-tolerant `_yaml_loader`) / **#108** (`parse_evidence` completeness guard). #94 reconciled Shipped in this note's commit (shipped 2026-07-02, row never flipped). Housekeeping, no row: an external reviewer's Codex session left its own active Work Log in `work/` (stale lock already recovered); archive or resume at next convenience. **DONE 2026-08-12** — archived verbatim to `archive/chore-govern-audit-20260808-20260812.md` with a chain entry. **The condition recurred immediately**: the reviewer session for that very PR left `chore-archive-codex-review-log.{md,lock.json}` in `work/` under `Owner: codex-root-pr401`, deriving the *same* worklog-key from the shared branch. Deliberately no row — AGENTS.md §Multi-Person already prescribes `<owner>-<worklog-key>.md` for exactly this, so it is an adherence gap in the reviewer's setup, not a missing mechanism, and this repo has already decided against adding machinery for existing-and-ignored rules. Expect the residue after any external review on a shared branch until that setup changes.

## Status Key

- Pending: not yet started
- In Progress: spec generated, bootstrap running
- Shipped: feature shipped (see Ship History in current_state.md)
- Deferred: explicitly deferred
