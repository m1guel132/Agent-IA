# /test

Execute the canonical workflow: `.agent/workflows/test.md`

## Required reads before execution

1. Active Work Log — must contain implementation evidence

## Execution

Follow every step in `.agent/workflows/test.md` sequentially.
Execute the minimal relevant test set and report commands + results.
If tests cannot run, state blockers explicitly.
End response with ⚡ ACX.
