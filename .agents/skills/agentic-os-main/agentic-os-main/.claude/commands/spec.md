# /spec

Execute the canonical workflow: `.agent/workflows/spec.md`

## Required reads before execution

1. `.agentcortex/context/current_state.md` — SSoT

## Execution

Follow every step in `.agent/workflows/spec.md` sequentially.
The user's task description is: $ARGUMENTS

- Convert requirements into verifiable specs.
- Verify consistency with existing `docs/specs/`.
- End response with ⚡ ACX.
