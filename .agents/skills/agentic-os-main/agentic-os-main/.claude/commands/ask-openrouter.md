# /ask-openrouter

Execute the canonical workflow: `.agent/workflows/ask-openrouter.md`

## Execution

Follow every step in `.agent/workflows/ask-openrouter.md` sequentially.
The user's task description is: $ARGUMENTS

- [OPTIONAL MODULE] Requires globally installed `ask-openrouter` CLI.
- If CLI is unavailable, inform the user and fall back to native execution.
- End response with ⚡ ACX.
