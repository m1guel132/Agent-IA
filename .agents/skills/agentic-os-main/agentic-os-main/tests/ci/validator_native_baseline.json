{
  "_doc": "ADR-006 ratchet baseline. Counting rule: non-comment, line-leading 'record_result ' call sites in validate.sh and 'Add-Result ' in validate.ps1 (regex ^\\s*record_result / ^\\s*Add-Result). Helper-internal calls are a constant offset absorbed here. New checks MUST go through run_python_check / Invoke-PythonCheck (which add zero such lines). Raising a count above original_floor requires a one-line entry in 'justifications' naming the check and why it must run without Python (ADR-006 escape hatch). Lowering counts (migration) requires updating these numbers in the same change. KNOWN SCOPE LIMIT: mid-line emission styles (cmd && record_result / || { record_result } / if(){ Add-Result }) are deliberately uncounted — that authoring style for NEW checks evades the machine ratchet and is caught at review (spec Enforcement Boundary; 3 such pre-existing sites live today (was 4; backlog #174 converted the docs/specs frontmatter PASS to a line-leading branch, which is why validate.sh is +2 below)).",
  "original_floor": {
    "validate_sh": 187,
    "validate_ps1": 188,
    "frozen": "2026-06-10"
  },
  "baseline": {
    "validate_sh": 204,
    "validate_ps1": 204
  },
  "justifications": [
    "token lifecycle drift advisory (issue #157 / backlog #51): 5 native record_result + 5 native Add-Result sites emit WARN/PASS/SKIP for governance token-cost drift. Must be WARN (advisory, never FAIL); run_python_check / Invoke-PythonCheck map exit!=0 -> FAIL and cannot express WARN. Mirrors the governance-eval-coverage advisory precedent. Hard teeth live in tests/ci/test_lifecycle_baseline_drift.py.",
    "downstream adaptability (ADR-007/008): +2 native record_result (validate.sh) and +2 Add-Result (validate.ps1) = the tool-absent SKIP branches for 'downstream-capabilities gate-safety' and 'safety nucleus freshness'. The actual checks run through run_python_check / Invoke-PythonCheck (zero added native lines); only the SKIP-when-the-deployed-tool-is-missing verdict is native, because the wrappers map a missing tool to FAIL while the correct downstream verdict on a partial/old install is SKIP (safe-to-ignore), exactly as the compact-index-freshness SKIP precedent.",
    "AC-6 current-branch gate-evidence FAIL (dev-flow-hardening spec): +1 record_result FAIL (validate.sh) and +1 Add-Result FAIL (validate.ps1) for the single combined 'current-branch work log missing Resume block and/or Test Gate Results at handoff/ship' finding. Must be FAIL-tier native (not Python): Python check already implements the gate-progression backstop; the new finding is a complementary structural check on Resume/Test-Gate-Results presence that must emit FAIL (not WARN) to match the AC-6 spec requirement. Single combined counter avoids the count growing proportionally with sub-checks.",
    "routing_actions stale-pending advisory (backlog #97): +1 native WARN in validate.sh and +1 Add-Result WARN in validate.ps1 because the check must work in no-python/reduced-assurance validation and emit WARN, not FAIL; the existing Python wrapper cannot express an advisory-only stale snapshot result.",
    "INDEX.jsonl referenced-file existence (governance self-audit D4): +1 native record_result (validate.sh) and +1 Add-Result (validate.ps1) for the single computed-level check that each INDEX.jsonl `log` entry resolves to a file on disk. Must be native WARN, not run_python_check FAIL: the hash chain + git witness forbid deleting an entry, so a genuine historical dangling reference cannot be cleanly removed and must SURFACE (WARN) rather than block (FAIL). Single computed-level Add-Result/record_result call avoids +2. Complements audit-chain integrity (which checks link continuity, not artifact existence).",
    "Work Log enforcement audit #288 (backlog #89): +2 native record_result (validate.sh) and +2 Add-Result (validate.ps1) — a WARN/PASS pair for each of two artifact-presence audits: (a) `## Security Findings` section on feature-tier logs at review/ship (security_guardrails.md §Work Log), and (b) `Guardrails loaded:` receipt in the current-branch log (engineering_guardrails.md bootstrap). Must be native WARN, not run_python_check FAIL: both are advisory presence audits (not proof of behavior), and the Python wrappers map exit!=0 -> FAIL and cannot express WARN — the same advisory precedent as the token-lifecycle-drift and routing_actions entries above. Logic is trivial heading/line presence (grep -qE / -match), so parity risk is minimal and is pinned by tests/ci/test_validator_worklog_enforcement.py.",
    "active work-log family SKIP (backlog #149): +1 native record_result (validate.sh) and +1 Add-Result (validate.ps1) emitting SKIP when .agentcortex/context/work/ holds no *.md log. The 18 checks in that family are each guarded by `worklog_count -gt 0` / `$worklogs.Count -gt 0`, so with zero logs the whole family emitted NOTHING — not SKIP, absent — while the summary still printed 'integrity check passed'; a fresh clone therefore ran ~18 fewer checks and was told everything passed (measured 2026-07-27: pass=99 with no logs vs pass=117 with one, same commit). Must be native, not run_python_check: (a) the wrappers map exit!=0 -> FAIL and cannot express SKIP, the same constraint cited in the ADR-007/008 tool-absent SKIP entry above; (b) the count it reports on is computed by the surrounding native loop, so a Python tool would have to re-implement the entire family to know it. One family-level emission, not one per check, so the count does not grow with the family. Pinned by tests/ci/test_validator_worklog_family_skip.py (red/green on both validators).",
    "docs/specs governed-spec SKIP (backlog #174): validate.sh +2, validate.ps1 +1. The asymmetry is real and worth stating: sh's PASS was previously a MID-LINE emission (`[[ cond ]] && record_result PASS`), which this counting rule deliberately does not count, so converting it to a line-leading if/elif/else added one counted line on top of the new SKIP; ps1's PASS was already line-leading, so only the SKIP is new there. The SKIP itself: emitted when docs/specs/ holds no GOVERNED spec (meta/_* and .gitkeep.md excluded). The PASS it now guards was vacuous — a bare glob counted the `_*` meta files the scanning loop had just skipped (NOT `.gitkeep.md` -- bash `*.md` never matches a dotfile without dotglob; an earlier draft of this note said otherwise and was wrong), so a downstream that had run /spec-intake but written no spec was told its spec frontmatter was valid over zero specs. Dropping that PASS without emitting anything would have traded one defect for the other named in justification #7 above (backlog #149: a check emitting NOTHING while the summary still prints \"integrity check passed\" IS the defect). Must be native, not run_python_check: the wrappers map exit!=0 to FAIL and cannot express SKIP — the same constraint cited in the ADR-007/008 and #149 entries."
  ]
}
